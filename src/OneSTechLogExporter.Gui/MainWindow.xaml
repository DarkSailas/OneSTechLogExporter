<Window x:Class="OneSTechLogExporter.Gui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="1С Log Viewer &amp; Exporter" Height="780" Width="1320"
        MinHeight="540" MinWidth="960" WindowState="Maximized"
        WindowStartupLocation="CenterScreen" Background="#141414" Foreground="#FFFFFF"
        FontFamily="Segoe UI, Roboto, sans-serif"
        ToolTipService.InitialShowDelay="2000"
        ToolTipService.ShowDuration="30000"
        ToolTipService.BetweenShowDelay="200"
        UseLayoutRounding="True" TextOptions.TextFormattingMode="Display">
    
    <Window.Resources>
        <!-- Superlative Industrial Monograph Design Tokens -->
        <SolidColorBrush x:Key="InkBlack" Color="#141414"/>
        <SolidColorBrush x:Key="CharcoalPress" Color="#1F1F1F"/>
        <SolidColorBrush x:Key="StudioSurface" Color="#262626"/>
        <SolidColorBrush x:Key="ElevatedSurface" Color="#2E2E2E"/>
        <SolidColorBrush x:Key="HairlineBorder" Color="#333333"/>
        <SolidColorBrush x:Key="ActiveBorder" Color="#4D4D4D"/>
        
        <!-- Typography -->
        <SolidColorBrush x:Key="Vapor" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="PulpCream" Color="#F6F4F2"/>
        <SolidColorBrush x:Key="GraphiteVeil" Color="#E4E3E2"/>
        <SolidColorBrush x:Key="MidAsh" Color="#8C8C8C"/>
        
        <!-- Hardware LED Accent -->
        <SolidColorBrush x:Key="EmberDiode" Color="#E66F27"/>
        <SolidColorBrush x:Key="EmberHover" Color="#F08038"/>
        <SolidColorBrush x:Key="EmberDim" Color="#4A250D"/>

        <!-- ContextMenu в стиле Superlative Industrial (0px radius, 1px sharp border) -->
        <Style TargetType="ContextMenu">
            <Setter Property="Background" Value="#1C1C1C"/>
            <Setter Property="Foreground" Value="#F6F4F2"/>
            <Setter Property="BorderBrush" Value="#383838"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="#1C1C1C" BorderBrush="#383838" BorderThickness="1" CornerRadius="0" Padding="4">
                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="MenuItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#F6F4F2"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="ItemBorder" Background="{TemplateBinding Background}" CornerRadius="2" Padding="{TemplateBinding Padding}" Margin="0,1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" MinWidth="22" SharedSizeGroup="IconGroup"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto" SharedSizeGroup="ShortcutGroup"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter x:Name="Icon" ContentSource="Icon" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <ContentPresenter Grid.Column="1" ContentSource="Header" RecognizesAccessKey="True" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="2" Text="{TemplateBinding InputGestureText}" Foreground="#8C8C8C" Margin="16,0,4,0" VerticalAlignment="Center" FontSize="11" FontFamily="Consolas, monospace"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#2A2A2A"/>
                                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="#E66F27"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#555555"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Separator">
            <Setter Property="Height" Value="1"/>
            <Setter Property="Margin" Value="4,4"/>
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Separator">
                        <Border Background="{TemplateBinding Background}" Height="{TemplateBinding Height}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Разделитель столбцов таблицы для изменения ширины мышью -->
        <Style x:Key="ColumnHeaderGripperStyle" TargetType="Thumb">
            <Setter Property="Width" Value="6"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Cursor" Value="SizeWE"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Заголовки столбцов таблицы DataGrid (Superlative Industrial Spec Header) -->
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="#8C8C8C"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="11.5"/>
            <Setter Property="Padding" Value="8,7,12,7"/>
            <Setter Property="BorderBrush" Value="#2B2B2B"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridColumnHeader">
                        <Grid SnapsToDevicePixels="True">
                            <Border x:Name="HeaderBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                                <DockPanel LastChildFill="True" VerticalAlignment="Center">
                                    <Path x:Name="SortArrow" DockPanel.Dock="Right" Width="8" Height="6" Margin="6,0,0,0" VerticalAlignment="Center" Fill="#E66F27" Stretch="Fill" Visibility="Collapsed"/>
                                    <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                </DockPanel>
                            </Border>
                            <Thumb x:Name="PART_RightHeaderGripper" HorizontalAlignment="Right" Style="{StaticResource ColumnHeaderGripperStyle}"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="HeaderBorder" Property="Background" Value="#242424"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="SortDirection" Value="Ascending">
                                <Setter TargetName="HeaderBorder" Property="Background" Value="#222222"/>
                                <Setter TargetName="HeaderBorder" Property="BorderBrush" Value="#E66F27"/>
                                <Setter TargetName="SortArrow" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="SortArrow" Property="Data" Value="M 0,6 L 4,0 L 8,6 Z"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="SortDirection" Value="Descending">
                                <Setter TargetName="HeaderBorder" Property="Background" Value="#222222"/>
                                <Setter TargetName="HeaderBorder" Property="BorderBrush" Value="#E66F27"/>
                                <Setter TargetName="SortArrow" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="SortArrow" Property="Data" Value="M 0,0 L 4,6 L 8,0 Z"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Строки и ячейки таблицы DataGrid (Ink Black #141414 / Charcoal #1B1B1B) -->
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#242424"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#2D2D2D"/>
                    <Setter Property="Foreground" Value="#FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}" BorderThickness="0" Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#2D2D2D"/>
                    <Setter Property="Foreground" Value="#FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Вкладки TabItem (Superlative Industrial Spec Switcher, 3px radius) -->
        <Style TargetType="TabItem">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="#8C8C8C"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="14,6"/>
            <Setter Property="Margin" Value="0,0,4,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="#333333" BorderThickness="1" CornerRadius="3" Padding="{TemplateBinding Padding}" Margin="{TemplateBinding Margin}">
                            <ContentPresenter x:Name="contentPresenter" HorizontalAlignment="Center" VerticalAlignment="Center" ContentSource="Header"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#242424"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#262626"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#E66F27"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox в индустриальном стиле -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#1C1C1C"/>
            <Setter Property="Foreground" Value="#F6F4F2"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="true">
                                <Setter TargetName="Bd" Property="Background" Value="#2D2D2D"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="true">
                                <Setter TargetName="Bd" Property="Background" Value="#2D2D2D"/>
                                <Setter Property="Foreground" Value="#E66F27"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="#F6F4F2"/>
            <Setter Property="BorderBrush" Value="#333333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid x:Name="MainGrid" SnapsToDevicePixels="true">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="20"/>
                            </Grid.ColumnDefinitions>
                            <ToggleButton x:Name="ToggleButton" Grid.ColumnSpan="2" Focusable="false" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="templateRoot" Background="#1A1A1A" BorderBrush="#333333" BorderThickness="1" CornerRadius="3">
                                            <Border x:Name="splitBorder" Width="20" HorizontalAlignment="Right" BorderBrush="Transparent" BorderThickness="0">
                                                <Path x:Name="arrow" HorizontalAlignment="Center" VerticalAlignment="Center" Data="F1 M 0,0 L 4,4 L 8,0 Z" Fill="#8C8C8C"/>
                                            </Border>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="true">
                                                <Setter TargetName="templateRoot" Property="Background" Value="#242424"/>
                                                <Setter TargetName="templateRoot" Property="BorderBrush" Value="#E66F27"/>
                                                <Setter TargetName="arrow" Property="Fill" Value="#FFFFFF"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="contentTemplate" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Margin="{TemplateBinding Padding}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" IsHitTestVisible="false"/>
                            <Popup x:Name="PART_Popup" AllowsTransparency="true" Grid.ColumnSpan="2" IsOpen="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" Margin="1" PopupAnimation="Slide" Placement="Bottom">
                                <Border x:Name="dropDownBorder" Background="#1C1C1C" BorderBrush="#383838" BorderThickness="1" CornerRadius="0" MinWidth="{Binding ActualWidth, ElementName=MainGrid}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <ScrollViewer x:Name="DropDownScrollViewer">
                                        <Grid x:Name="grid" RenderOptions.ClearTypeHint="Enabled">
                                            <Canvas x:Name="canvas" Height="0" Width="0" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                                            <ItemsPresenter x:Name="ItemsPresenter" KeyboardNavigation.DirectionalNavigation="Contained" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                        </Grid>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Поля ввода TextBox и PasswordBox (3px radius, hairline #333333, focus #E66F27) -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="#F6F4F2"/>
            <Setter Property="BorderBrush" Value="#333333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontSize" Value="12.5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" Margin="2,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#E66F27"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="#F6F4F2"/>
            <Setter Property="BorderBrush" Value="#333333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontSize" Value="12.5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" Margin="2,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#E66F27"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стандартная аппаратная кнопка (Superlative Hardware Button, 3px radius) -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="Foreground" Value="#F6F4F2"/>
            <Setter Property="BorderBrush" Value="#383838"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,5"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#292929"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#E66F27"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#171717"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.35"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Ghost Button без обводки -->
        <Style x:Key="GhostButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#8C8C8C"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="3" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#242424"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ToggleButton меню фильтров (Superlative Spec Switcher) -->
        <Style x:Key="GhostToggleButton" TargetType="ToggleButton">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="Foreground" Value="#8C8C8C"/>
            <Setter Property="BorderBrush" Value="#383838"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#262626"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#E66F27"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#242424"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#E66F27"/>
                                <Setter Property="Foreground" Value="#E66F27"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Компактная кнопка-переключатель ВКЛЮЧЕНИЯ (ДА / +) для списков фильтрации по полям -->
        <Style x:Key="FilterIncludeToggleButton" TargetType="ToggleButton">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="#737373"/>
            <Setter Property="BorderBrush" Value="#383838"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="18"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="FontSize" Value="9.5"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#262626"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#4ADE80"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1C3824"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#22C55E"/>
                                <Setter Property="Foreground" Value="#4ADE80"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Компактная кнопка-переключатель ИСКЛЮЧЕНИЯ (НЕ / -) для списков фильтрации по полям -->
        <Style x:Key="FilterExcludeToggleButton" TargetType="ToggleButton">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="#737373"/>
            <Setter Property="BorderBrush" Value="#383838"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="18"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="FontSize" Value="9.5"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#262626"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#E66F27"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3D1B16"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#E66F27"/>
                                <Setter Property="Foreground" Value="#E66F27"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary Action Button (Superlative Outlined Ghost BUY NOW Spec: 1px border, 3px radius) -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#E66F27"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#E66F27"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,5"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F08038"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#F08038"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#CC5D1B"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.35"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Кнопка инспекции строки в таблице (Superlative Spec Button, 3px radius) -->
        <Style x:Key="InspectButton" TargetType="Button">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="#8C8C8C"/>
            <Setter Property="BorderBrush" Value="#333333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,2"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#292929"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#E66F27"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Глобальный стиль всплывающих подсказок ToolTip с автоматическим переносом слов -->
        <Style TargetType="ToolTip">
            <Setter Property="Background" Value="#1C1C1C"/>
            <Setter Property="Foreground" Value="#F6F4F2"/>
            <Setter Property="BorderBrush" Value="#E66F27"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,9"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="HasDropShadow" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToolTip">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3" Padding="{TemplateBinding Padding}" MaxWidth="560">
                            <TextBlock Text="{TemplateBinding Content}" TextWrapping="Wrap" Foreground="{TemplateBinding Foreground}" FontSize="12" LineHeight="16"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Индикатор / Иконка Справки (InfoBadge) -->
        <Style x:Key="InfoBadge" TargetType="ContentControl">
            <Setter Property="Width" Value="14"/>
            <Setter Property="Height" Value="14"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="5,0,0,0"/>
            <Setter Property="Cursor" Value="Help"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="ToolTipService.InitialShowDelay" Value="2000"/>
            <Setter Property="ToolTipService.ShowDuration" Value="30000"/>
            <Setter Property="ToolTipService.BetweenShowDelay" Value="200"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContentControl">
                        <Border x:Name="Bd" Background="#222222" BorderBrush="#444444" BorderThickness="1" CornerRadius="7">
                            <TextBlock Text="i" Foreground="{StaticResource MidAsh}" FontSize="9" FontWeight="Bold" FontFamily="Segoe UI, Tahoma, sans-serif" HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center" Margin="0,0,0,1"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#332218"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#E66F27"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid x:Name="MainRootGrid">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Шапка окна (Superlative Industrial Spec Header) -->
            <Border Grid.Row="0" Background="{StaticResource CharcoalPress}" CornerRadius="0" Padding="14,10" Margin="0,0,0,8" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Background="#141414" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1" CornerRadius="15" Padding="9,2" Margin="0,0,10,0">
                            <TextBlock Text="1C LOGS" Foreground="{StaticResource EmberDiode}" FontWeight="SemiBold" FontSize="10"/>
                        </Border>
                        <TextBlock Text="1С Log Viewer &amp; Exporter" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource Vapor}" VerticalAlignment="Center"/>
                        <TextBlock Text="— Мониторинг, анализ и экспорт технологического журнала и журнала регистрации" FontSize="11.5" Foreground="{StaticResource MidAsh}" VerticalAlignment="Center" Margin="8,0,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button x:Name="BtnSaveConfig" Content="СОХРАНИТЬ НАСТРОЙКИ" Click="BtnSaveConfig_Click" Margin="0,0,6,0"/>
                        <Button x:Name="BtnSaveJson" Content="ЭКСПОРТ JSON" Click="BtnSaveJson_Click" Margin="0,0,6,0"/>
                        <Button x:Name="BtnExportAllElastic" Content="ЭКСПОРТ В ELASTIC" Click="BtnExportAllElastic_Click" Style="{StaticResource PrimaryButton}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Основной Контент с Вкладками -->
            <TabControl x:Name="MainTabControl" Grid.Row="1" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Stretch" HorizontalContentAlignment="Stretch">
                <!-- Вкладка Технологический Журнал (ТЖ) -->
                <TabItem Header="ТЕХНОЛОГИЧЕСКИЙ ЖУРНАЛ (ТЖ)">
                    <Grid Margin="0,6,0,0" VerticalAlignment="Stretch">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Панель Загрузки и Парсинга ТЖ -->
                        <Grid Grid.Row="0" Margin="0,0,0,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,6,0">
                                <TextBlock Text="КАТАЛОГ ТЖ:" Foreground="{StaticResource MidAsh}" FontSize="11" FontWeight="SemiBold"/>
                                <ContentControl Style="{StaticResource InfoBadge}" ToolTip="Каталог логов ТЖ 1С, содержащий подпапки рабочих процессов (rphost_*, rmngr_*, ragent_*). Поддерживается глубокий рекурсивный поиск логов."/>
                            </StackPanel>
                            <TextBox x:Name="TxtTgPath" Grid.Column="1" Text="C:\Logs\TGLogs" VerticalAlignment="Center"/>
                            <Button x:Name="BtnBrowseTg" Grid.Column="2" Content="ОБЗОР..." Click="BtnBrowseTg_Click" Margin="6,0,0,0"/>
                            <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,4,0">
                                <TextBlock Text="ЛИМИТ:" Foreground="{StaticResource MidAsh}" FontSize="11" FontWeight="SemiBold"/>
                                <ContentControl Style="{StaticResource InfoBadge}" ToolTip="Ограничение количества загружаемых строк для быстрого просмотра и мгновенной диагностики без перегрузки оперативной памяти."/>
                            </StackPanel>
                            <ComboBox x:Name="CmbTgLimit" Grid.Column="4" Width="110" VerticalAlignment="Center" SelectedIndex="5">
                                <ComboBoxItem Content="1 000"/>
                                <ComboBoxItem Content="2 000"/>
                                <ComboBoxItem Content="5 000"/>
                                <ComboBoxItem Content="10 000"/>
                                <ComboBoxItem Content="50 000"/>
                                <ComboBoxItem Content="Все записи"/>
                            </ComboBox>
                            <Button x:Name="BtnParseTg" Grid.Column="5" Content="РАСПАРСИТЬ ТЖ" Click="BtnParseTg_Click" Style="{StaticResource PrimaryButton}" ToolTip="Запустить высокоскоростной параллельный разбор файлов Технологического Журнала" Margin="6,0,0,0"/>
                            <Button x:Name="BtnCancelTg" Grid.Column="6" Content="ОСТАНОВИТЬ" Click="BtnCancelParsing_Click" Visibility="Collapsed" Background="#331414" Foreground="#FFFFFF" BorderBrush="#8A2B2B" BorderThickness="1" Padding="12,5" ToolTip="Остановить выполнение операции парсинга" Margin="6,0,0,0"/>
                            <Button x:Name="BtnExportTgJson" Grid.Column="7" Content="JSON" Click="BtnExportTgJson_Click" ToolTip="Экспорт выделенных строк (или всей таблицы) ТЖ в JSON" Margin="6,0,0,0"/>
                            <Button x:Name="BtnExportTgExcel" Grid.Column="8" Content="EXCEL" Click="BtnExportTgExcel_Click" ToolTip="Экспорт таблицы ТЖ в файл Microsoft Excel (.xlsx)" Margin="6,0,0,0"/>
                            <Button x:Name="BtnExportTgElastic" Grid.Column="9" Content="В ELASTIC" Click="BtnExportTgElastic_Click" ToolTip="Экспортировать текущие отображаемые записи таблицы напрямую в кластер Elasticsearch" Margin="6,0,0,0"/>
                        </Grid>

                        <!-- Панель Поиска, Фильтров и Сортировки ТЖ -->
                        <Border Grid.Row="1" Background="{StaticResource CharcoalPress}" CornerRadius="0" Padding="10,8" Margin="0,0,0,8" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1">
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" MinWidth="180" MaxWidth="320"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- 1. Быстрый Поиск с поддержкой исключений '!' и '-' -->
                                    <Grid Grid.Column="0" Margin="0,0,8,0">
                                        <TextBox x:Name="TxtTgFilter" TextChanged="TxtTgFilter_TextChanged" Padding="8,6,8,6" FontSize="12" ToolTip="Быстрый поиск по всем полям. Для исключения используйте ! или - (например: !ATTN, -ragent, !Администратор)"/>
                                    </Grid>

                                    <!-- 2. Фильтр по времени (С / По) -->
                                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                                        <TextBlock Text="ВРЕМЯ:" VerticalAlignment="Center" Foreground="{StaticResource MidAsh}" FontSize="11" FontWeight="SemiBold" Margin="0,0,4,0"/>
                                        <TextBlock Text="с" VerticalAlignment="Center" Foreground="{StaticResource MidAsh}" FontSize="11" Margin="0,0,2,0"/>
                                        <TextBox x:Name="TxtTgTimeFrom" TextChanged="TxtTgTimeFilter_TextChanged" Width="72" ToolTip="Время С (например 16:13:00, 16:13 или 2026-08-17 16:13)" FontSize="12" Padding="4,4"/>
                                        <TextBlock Text="по" VerticalAlignment="Center" Foreground="{StaticResource MidAsh}" FontSize="11" Margin="4,0,2,0"/>
                                        <TextBox x:Name="TxtTgTimeTo" TextChanged="TxtTgTimeFilter_TextChanged" Width="72" ToolTip="Время ПО (например 16:15:00 или 16:15)" FontSize="12" Padding="4,4"/>
                                    </StackPanel>

                                    <!-- 3. Основной фильтр событий 1С -->
                                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                                        <TextBlock Text="СОБЫТИЕ:" VerticalAlignment="Center" Foreground="{StaticResource MidAsh}" FontSize="11" FontWeight="SemiBold" Margin="0,0,4,0"/>
                                        <ComboBox x:Name="CmbTgEventFilter" SelectionChanged="CmbTgEventFilter_SelectionChanged" Width="195" SelectedIndex="0">
                                            <ComboBoxItem Content="Все события 1С"/>
                                            <ComboBoxItem Content="Ошибки (EXCP / EXCPCNTX / QERR)"/>
                                            <ComboBoxItem Content="Блокировки (TLOCK / TDEADLOCK / TTIMEOUT)"/>
                                            <ComboBoxItem Content="Запросы СУБД (DBMSSQL / SDBL / DBPOSTGRS)"/>
                                            <ComboBoxItem Content="Вызовы (CALL / SCALL / RUNMETH)"/>
                                            <ComboBoxItem Content="Длительные (LONGDURATIONINFO)"/>
                                        </ComboBox>
                                    </StackPanel>

                                    <!-- 4. Меню Включающих Параметров Фильтрации -->
                                    <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                                        <ToggleButton x:Name="BtnTgFilterMenu" Content="ДОП. ФИЛЬТРЫ ▼" Style="{StaticResource GhostToggleButton}" Padding="9,5" PreviewMouseLeftButtonDown="MenuToggleButton_PreviewMouseLeftButtonDown" Click="MenuToggleButton_Click"/>
                                        <Popup IsOpen="{Binding IsChecked, ElementName=BtnTgFilterMenu}" StaysOpen="False" Closed="MenuPopup_Closed" PlacementTarget="{Binding ElementName=BtnTgFilterMenu}" Placement="Bottom" AllowsTransparency="True">
                                            <Border Background="{StaticResource StudioSurface}" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1" CornerRadius="0" Padding="14" Margin="0,4,0,0">
                                                <StackPanel Width="360">
                                                    <TextBlock Text="ДОПОЛНИТЕЛЬНЫЕ ПАРАМЕТРЫ ФИЛЬТРАЦИИ ТЖ" FontWeight="SemiBold" Foreground="{StaticResource Vapor}" FontSize="12" Margin="0,0,0,10"/>

                                                    <TextBlock Text="СТАТУС ВЫПОЛНЕНИЯ:" Foreground="{StaticResource MidAsh}" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                                    <WrapPanel Margin="0,0,0,10">
                                                        <CheckBox x:Name="ChkTgIncRunning" Content="Выполняются (LONGDURATION)" Foreground="{StaticResource Vapor}" Margin="0,0,12,4" Click="ChkTgFilter_Click"/>
                                                        <CheckBox x:Name="ChkTgIncCompleted" Content="Завершенные" Foreground="{StaticResource Vapor}" Margin="0,0,0,4" Click="ChkTgFilter_Click"/>
                                                    </WrapPanel>

                                                    <TextBlock Text="МИНИМАЛЬНАЯ ДЛИТЕЛЬНОСТЬ:" Foreground="{StaticResource MidAsh}" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                                    <ComboBox x:Name="CmbTgMinDuration" SelectionChanged="CmbTgMinDuration_SelectionChanged" SelectedIndex="0" Margin="0,0,0,10">
                                                        <ComboBoxItem Content="Любая длительность (без порога)"/>
                                                        <ComboBoxItem Content="Дольше 10 мс"/>
                                                        <ComboBoxItem Content="Дольше 100 мс"/>
                                                        <ComboBoxItem Content="Дольше 1 сек"/>
                                                        <ComboBoxItem Content="Дольше 5 сек"/>
                                                        <ComboBoxItem Content="Дольше 10 сек"/>
                                                    </ComboBox>

                                                    <TextBlock Text="СОРТИРОВКА ТАБЛИЦЫ:" Foreground="{StaticResource MidAsh}" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                                    <ComboBox x:Name="CmbTgSortPreset" SelectionChanged="CmbTgSortPreset_SelectionChanged" SelectedIndex="0">
                                                        <ComboBoxItem Content="Дата (сначала новые)"/>
                                                        <ComboBoxItem Content="Дата (сначала старые)"/>
                                                        <ComboBoxItem Content="Длительность (сначала долгие)"/>
                                                        <ComboBoxItem Content="Длительность + Новые дата"/>
                                                        <ComboBoxItem Content="Событие + Длительность"/>
                                                        <ComboBoxItem Content="Пользователь + Дата"/>
                                                    </ComboBox>
                                                </StackPanel>
                                            </Border>
                                        </Popup>
                                    </StackPanel>

                                    <!-- 5. Меню Исключающих Фильтров (НЕ) с множественным выбором -->
                                    <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                                        <ToggleButton x:Name="BtnTgExcludeMenu" Content="ИСКЛЮЧЕНИЯ (НЕ) ▼" Style="{StaticResource GhostToggleButton}" Padding="9,5" PreviewMouseLeftButtonDown="MenuToggleButton_PreviewMouseLeftButtonDown" Click="MenuToggleButton_Click"/>
                                        <Popup IsOpen="{Binding IsChecked, ElementName=BtnTgExcludeMenu}" StaysOpen="False" Closed="MenuPopup_Closed" PlacementTarget="{Binding ElementName=BtnTgExcludeMenu}" Placement="Bottom" AllowsTransparency="True">
                                            <Border Background="{StaticResource StudioSurface}" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1" CornerRadius="0" Padding="14" Margin="0,4,0,0">
                                                <StackPanel Width="420">
                                                    <TextBlock Text="ИСКЛЮЧЕНИЕ ДАННЫХ ИЗ ТАБЛИЦЫ (ФИЛЬТРЫ НЕ)" FontWeight="SemiBold" Foreground="{StaticResource Vapor}" FontSize="12" Margin="0,0,0,10"/>

                                                    <!-- Шапка секции событий с поиском и кнопками -->
                                                    <Grid Margin="0,0,0,6">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Text="Исключить события 1С:" VerticalAlignment="Center" Foreground="{StaticResource MidAsh}" FontSize="11" FontWeight="SemiBold"/>
                                                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                                                            <Button Content="СНЯТЬ ВСЕ" Click="BtnTgExcludeClearAll_Click" Style="{StaticResource GhostButton}" Padding="6,2" FontSize="10"/>
                                                        </StackPanel>
                                                    </Grid>

                                                    <TextBox x:Name="TxtTgExcludeEventSearch" TextChanged="TxtTgExcludeEventSearch_TextChanged" Padding="8,4" Margin="0,0,0,6" FontSize="11.5" ToolTip="Фильтр по списку событий..."/>

                                                    <!-- Скроллируемый список событий с чекбоксами -->
                                                    <Border Background="{StaticResource CharcoalPress}" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1" CornerRadius="0" Height="190" Margin="0,0,0,10">
                                                        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                                            <ItemsControl x:Name="ListTgExcludeEvents" Margin="6,4">
                                                                <ItemsControl.ItemTemplate>
                                                                    <DataTemplate>
                                                                        <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}" Margin="2,3" Click="ChkTgExcludeEventItem_Click">
                                                                            <CheckBox.Content>
                                                                                <TextBlock TextWrapping="NoWrap" TextTrimming="CharacterEllipsis">
                                                                                    <Run Text="{Binding Title}" FontWeight="SemiBold" Foreground="{StaticResource Vapor}"/>
                                                                                    <Run Text=" — " Foreground="{StaticResource MidAsh}"/>
                                                                                    <Run Text="{Binding Subtitle}" Foreground="{StaticResource MidAsh}" FontSize="11"/>
                                                                                </TextBlock>
                                                                            </CheckBox.Content>
                                                                        </CheckBox>
                                                                    </DataTemplate>
                                                                </ItemsControl.ItemTemplate>
                                                            </ItemsControl>
                                                        </ScrollViewer>
                                                    </Border>

                                                    <!-- Исключение процессов -->
                                                    <TextBlock Text="ИСКЛЮЧИТЬ ПРОЦЕССЫ 1С:" Foreground="{StaticResource MidAsh}" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                                    <WrapPanel Margin="0,0,0,10">
                                                        <CheckBox x:Name="ChkTgExRphost" Content="rphost" Foreground="{StaticResource Vapor}" Margin="0,0,12,4" Click="ChkTgFilter_Click"/>
                                                        <CheckBox x:Name="ChkTgExRmngr" Content="rmngr" Foreground="{StaticResource Vapor}" Margin="0,0,12,4" Click="ChkTgFilter_Click"/>
                                                        <CheckBox x:Name="ChkTgExRagent" Content="ragent" Foreground="{StaticResource Vapor}" Margin="0,0,0,4" Click="ChkTgFilter_Click"/>
                                                    </WrapPanel>

                                                    <!-- Исключение статусов -->
                                                    <TextBlock Text="ИСКЛЮЧИТЬ СТАТУСЫ ОПЕРАЦИЙ:" Foreground="{StaticResource MidAsh}" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                                    <WrapPanel Margin="0,0,0,10">
                                                        <CheckBox x:Name="ChkTgExCompleted" Content="Завершенные" Foreground="{StaticResource Vapor}" Margin="0,0,12,4" Click="ChkTgFilter_Click"/>
                                                        <CheckBox x:Name="ChkTgExRunning" Content="Выполняющиеся" Foreground="{StaticResource Vapor}" Margin="0,0,0,4" Click="ChkTgFilter_Click"/>
                                                    </WrapPanel>

                                                    <!-- Памятка по строке поиска -->
                                                    <Border Background="#141414" CornerRadius="0" Padding="8,6" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1">
                                                        <TextBlock Foreground="{StaticResource MidAsh}" FontSize="11" TextWrapping="Wrap" LineHeight="15">
                                                            <Run FontWeight="SemiBold" Foreground="{StaticResource Vapor}">РУЧНЫЕ ИСКЛЮЧЕНИЯ:</Run> вводите в поиск <Run FontWeight="Bold" Foreground="{StaticResource EmberDiode}">!</Run> перед словом (например <Run Foreground="{StaticResource Vapor}">!DefUser</Run>, <Run Foreground="{StaticResource Vapor}">!1Cv8C</Run>, <Run Foreground="{StaticResource Vapor}">!ServerJobExecutorContext</Run>).
                                                        </TextBlock>
                                                    </Border>
                                                </StackPanel>
                                            </Border>
                                        </Popup>
                                    </StackPanel>

                                    <!-- 6. Меню Отбора по заполненным полям ТЖ (Пользователи, Приложения, PID, SPID, OSThread) -->
                                    <StackPanel Grid.Column="5" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                                        <ToggleButton x:Name="BtnTgFieldsMenu" Content="ОТБОР ПО ПОЛЯМ ▼" Style="{StaticResource GhostToggleButton}" Padding="9,5" PreviewMouseLeftButtonDown="MenuToggleButton_PreviewMouseLeftButtonDown" Click="MenuToggleButton_Click"/>
                                        <Popup IsOpen="{Binding IsChecked, ElementName=BtnTgFieldsMenu}" StaysOpen="False" Closed="MenuPopup_Closed" PlacementTarget="{Binding ElementName=BtnTgFieldsMenu}" Placement="Bottom" AllowsTransparency="True">
                                            <Border Background="{StaticResource StudioSurface}" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1" CornerRadius="0" Padding="14" Margin="0,4,0,0">
                                                <StackPanel Width="560">
                                                    <Grid Margin="0,0,0,8">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Text="ОТБОР ЗАПИСЕЙ ТЖ ПО ПОЛЯМ" FontWeight="SemiBold" Foreground="{StaticResource Vapor}" FontSize="12" VerticalAlignment="Center"/>
                                                        <Button Grid.Column="1" Content="СБРОСИТЬ ВСЕ" Click="BtnTgClearAllFieldFilters_Click" Style="{StaticResource GhostButton}" Padding="6,2" FontSize="10"/>
                                                    </Grid>

                                                    <TabControl Background="Transparent" BorderThickness="0" Height="270">
                                                        <TabItem Header="ПОЛЬЗОВАТЕЛИ">
                                                            <Grid Margin="0,6,0,0">
                                                                <Grid.RowDefinitions>
                                                                    <RowDefinition Height="Auto"/>
                                                                    <RowDefinition Height="*"/>
                                                                </Grid.RowDefinitions>
                                                                <Grid Grid.Row="0" Margin="0,0,0,6">
                                                                    <Grid.ColumnDefinitions>
                                                                        <ColumnDefinition Width="*"/>
                                                                        <ColumnDefinition Width="Auto"/>
                                                                    </Grid.ColumnDefinitions>
                                                                    <TextBox x:Name="TxtTgSearchUsers" TextChanged="TxtTgSearchUsers_TextChanged" Padding="6,3" FontSize="11.5" ToolTip="Поиск пользователя..."/>
                                                                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="6,0,0,0">
                                                                        <Button Content="+ ВСЕ" Click="BtnTgSelectAllUsers_Click" Style="{StaticResource GhostButton}" Padding="5,2" FontSize="9.5" Margin="0,0,3,0" ToolTip="Включить все видимые"/>
                                                                        <Button Content="НЕ ВСЕ" Click="BtnTgExcludeAllUsers_Click" Style="{StaticResource GhostButton}" Padding="5,2" FontSize="9.5" Margin="0,0,3,0" ToolTip="Исключить все видимые"/>
                                                                        <Button Content="СНЯТЬ" Click="BtnTgClearUsers_Click" Style="{StaticResource GhostButton}" Padding="5,2" FontSize="9.5" ToolTip="Снять отметки"/>
                                                                    </StackPanel>
                                                                </Grid>
                                                                <Border Grid.Row="1" Background="{StaticResource CharcoalPress}" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1" CornerRadius="0">
                                                                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                                                        <ItemsControl x:Name="ListTgUsers" Margin="6,4">
                                                                            <ItemsControl.ItemTemplate>
                                                                                <DataTemplate>
                                                                                    <Grid Margin="1,2">
                                                                                        <Grid.ColumnDefinitions>
                                                                                            <ColumnDefinition Width="Auto"/>
                                                                                            <ColumnDefinition Width="Auto"/>
                                                                                            <ColumnDefinition Width="*"/>
                                                                                        </Grid.ColumnDefinitions>
                                                                                        <ToggleButton IsChecked="{Binding IsInclude, Mode=TwoWay}" Click="ChkTgFieldValueItem_Click"
                                                                                                      Content="ДА" Style="{StaticResource FilterIncludeToggleButton}" ToolTip="Включить в отбор (только это значение)"/>
                                                                                        <ToggleButton Grid.Column="1" IsChecked="{Binding IsExclude, Mode=TwoWay}" Click="ChkTgFieldValueItem_Click"
                                                                                                      Content="НЕ" Style="{StaticResource FilterExcludeToggleButton}" Margin="3,0,6,0" ToolTip="Исключить из отбора (скрыть это значение)"/>
                                                                                        <TextBlock Grid.Column="2" Text="{Binding DisplayText}" Foreground="{StaticResource Vapor}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                                                                    </Grid>
                                                                                </DataTemplate>
                                                                            </ItemsControl.ItemTemplate>
                                                                        </ItemsControl>
                                                                    </ScrollViewer>
                                                                </Border>
                                                            </Grid>
                                                        </TabItem>

                                                        <TabItem Header="ПРИЛОЖЕНИЯ">
                                                            <Grid Margin="0,6,0,0">
                                                                <Grid.RowDefinitions>
                                                                    <RowDefinition Height="Auto"/>
                                                                    <RowDefinition Height="*"/>
                                                                </Grid.RowDefinitions>
                                                                <Grid Grid.Row="0" Margin="0,0,0,6">
                                                                    <Grid.ColumnDefinitions>
                                                                        <ColumnDefinition Width="*"/>
                                                                        <ColumnDefinition Width="Auto"/>
                                                                    </Grid.ColumnDefinitions>
                                                                    <TextBox x:Name="TxtTgSearchApps" TextChanged="TxtTgSearchApps_TextChanged" Padding="6,3" FontSize="11.5" ToolTip="Поиск приложения..."/>
                                                                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="6,0,0,0">
                                                                        <Button Content="+ ВСЕ" Click="BtnTgSelectAllApps_Click" Style="{StaticResource GhostButton}" Padding="5,2" FontSize="9.5" Margin="0,0,3,0" ToolTip="Включить все видимые"/>
                                                                        <Button Content="НЕ ВСЕ" Click="BtnTgExcludeAllApps_Click" Style="{StaticResource GhostButton}" Padding="5,2" FontSize="9.5" Margin="0,0,3,0" ToolTip="Исключить все видимые"/>
                                                                        <Button Content="СНЯТЬ" Click="BtnTgClearApps_Click" Style="{StaticResource GhostButton}" Padding="5,2" FontSize="9.5" ToolTip="Снять отметки"/>
                                                                    </StackPanel>
                                                                </Grid>
                                                                <Border Grid.Row="1" Background="{StaticResource CharcoalPress}" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1" CornerRadius="0">
                                                                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                                                        <ItemsControl x:Name="ListTgApps" Margin="6,4">
                                                                            <ItemsControl.ItemTemplate>
                                                                                <DataTemplate>
                                                                                    <Grid Margin="1,2">
                                                                                        <Grid.ColumnDefinitions>
                                                                                            <ColumnDefinition Width="Auto"/>
                                                                                            <ColumnDefinition Width="Auto"/>
                                                                                            <ColumnDefinition Width="*"/>
                                                                                        </Grid.ColumnDefinitions>
                                                                                        <ToggleButton IsChecked="{Binding IsInclude, Mode=TwoWay}" Click="ChkTgFieldValueItem_Click"
                                                                                                      Content="ДА" Style="{StaticResource FilterIncludeToggleButton}" ToolTip="Включить в отбор (только это значение)"/>
                                                                                        <ToggleButton Grid.Column="1" IsChecked="{Binding IsExclude, Mode=TwoWay}" Click="ChkTgFieldValueItem_Click"
                                                                                                      Content="НЕ" Style="{StaticResource FilterExcludeToggleButton}" Margin="3,0,6,0" ToolTip="Исключить из отбора (скрыть это значение)"/>
                                                                                        <TextBlock Grid.Column="2" Text="{Binding DisplayText}" Foreground="{StaticResource Vapor}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                                                                    </Grid>
                                                                                </DataTemplate>
                                                                            </ItemsControl.ItemTemplate>
                                                                        </ItemsControl>
                                                                    </ScrollViewer>
                                                                </Border>
                                                            </Grid>
                                                        </TabItem>

                                                        <TabItem Header="PID">
                                                            <Grid Margin="0,6,0,0">
                                                                <Grid.RowDefinitions>
                                                                    <RowDefinition Height="Auto"/>
                                                                    <RowDefinition Height="*"/>
                                                                </Grid.RowDefinitions>
                                                                <Grid Grid.Row="0" Margin="0,0,0,6">
                                                                    <Grid.ColumnDefinitions>
                                                                        <ColumnDefinition Width="*"/>
                                                                        <ColumnDefinition Width="Auto"/>
                                                                    </Grid.ColumnDefinitions>
                                                                    <TextBox x:Name="TxtTgSearchPids" TextChanged="TxtTgSearchPids_TextChanged" Padding="6,3" FontSize="11.5" ToolTip="Поиск PID..."/>
                                                                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="6,0,0,0">
                                                                        <Button Content="+ ВСЕ" Click="BtnTgSelectAllPids_Click" Style="{StaticResource GhostButton}" Padding="5,2" FontSize="9.5" Margin="0,0,3,0" ToolTip="Включить все видимые"/>
                                                                        <Button Content="НЕ ВСЕ" Click="BtnTgExcludeAllPids_Click" Style="{StaticResource GhostButton}" Padding="5,2" FontSize="9.5" Margin="0,0,3,0" ToolTip="Исключить все видимые"/>
                                                                        <Button Content="СНЯТЬ" Click="BtnTgClearPids_Click" Style="{StaticResource GhostButton}" Padding="5,2" FontSize="9.5" ToolTip="Снять отметки"/>
                                                                    </StackPanel>
                                                                </Grid>
                                                                <Border Grid.Row="1" Background="{StaticResource CharcoalPress}" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1" CornerRadius="0">
                                                                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                                                        <ItemsControl x:Name="ListTgPids" Margin="6,4">
                                                                            <ItemsControl.ItemTemplate>
                                                                                <DataTemplate>
                                                                                    <Grid Margin="1,2">
                                                                                        <Grid.ColumnDefinitions>
                                                                                            <ColumnDefinition Width="Auto"/>
                                                                                            <ColumnDefinition Width="Auto"/>
                                                                                            <ColumnDefinition Width="*"/>
                                                                                        </Grid.ColumnDefinitions>
                                                                                        <ToggleButton IsChecked="{Binding IsInclude, Mode=TwoWay}" Click="ChkTgFieldValueItem_Click"
                                                                                                      Content="ДА" Style="{StaticResource FilterIncludeToggleButton}" ToolTip="Включить в отбор (только это значение)"/>
                                                                                        <ToggleButton Grid.Column="1" IsChecked="{Binding IsExclude, Mode=TwoWay}" Click="ChkTgFieldValueItem_Click"
                                                                                                      Content="НЕ" Style="{StaticResource FilterExcludeToggleButton}" Margin="3,0,6,0" ToolTip="Исключить из отбора (скрыть это значение)"/>
                                                                                        <TextBlock Grid.Column="2" Text="{Binding DisplayText}" Foreground="{StaticResource Vapor}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                                                                    </Grid>
                                                                                </DataTemplate>
                                                                            </ItemsControl.ItemTemplate>
                                                                        </ItemsControl>
                                                                    </ScrollViewer>
                                                                </Border>
                                                            </Grid>
                                                        </TabItem>

                                                        <TabItem Header="SPID">
                                                            <Grid Margin="0,6,0,0">
                                                                <Grid.RowDefinitions>
                                                                    <RowDefinition Height="Auto"/>
                                                                    <RowDefinition Height="*"/>
                                                                </Grid.RowDefinitions>
                                                                <Grid Grid.Row="0" Margin="0,0,0,6">
                                                                    <Grid.ColumnDefinitions>
                                                                        <ColumnDefinition Width="*"/>
                                                                        <ColumnDefinition Width="Auto"/>
                                                                    </Grid.ColumnDefinitions>
                                                                    <TextBox x:Name="TxtTgSearchSpids" TextChanged="TxtTgSearchSpids_TextChanged" Padding="6,3" FontSize="11.5" ToolTip="Поиск SPID..."/>
                                                                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="6,0,0,0">
                                                                        <Button Content="+ ВСЕ" Click="BtnTgSelectAllSpids_Click" Style="{StaticResource GhostButton}" Padding="5,2" FontSize="9.5" Margin="0,0,3,0" ToolTip="Включить все видимые"/>
                                                                        <Button Content="НЕ ВСЕ" Click="BtnTgExcludeAllSpids_Click" Style="{StaticResource GhostButton}" Padding="5,2" FontSize="9.5" Margin="0,0,3,0" ToolTip="Исключить все видимые"/>
                                                                        <Button Content="СНЯТЬ" Click="BtnTgClearSpids_Click" Style="{StaticResource GhostButton}" Padding="5,2" FontSize="9.5" ToolTip="Снять отметки"/>
                                                                    </StackPanel>
                                                                </Grid>
                                                                <Border Grid.Row="1" Background="{StaticResource CharcoalPress}" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1" CornerRadius="0">
                                                                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                                                        <ItemsControl x:Name="ListTgSpids" Margin="6,4">
                                                                            <ItemsControl.ItemTemplate>
                                                                                <DataTemplate>
                                                                                    <Grid Margin="1,2">
                                                                                        <Grid.ColumnDefinitions>
                                                                                            <ColumnDefinition Width="Auto"/>
                                                                                            <ColumnDefinition Width="Auto"/>
                                                                                            <ColumnDefinition Width="*"/>
                                                                                        </Grid.ColumnDefinitions>
                                                                                        <ToggleButton IsChecked="{Binding IsInclude, Mode=TwoWay}" Click="ChkTgFieldValueItem_Click"
                                                                                                      Content="ДА" Style="{StaticResource FilterIncludeToggleButton}" ToolTip="Включить в отбор (только это значение)"/>
                                                                                        <ToggleButton Grid.Column="1" IsChecked="{Binding IsExclude, Mode=TwoWay}" Click="ChkTgFieldValueItem_Click"
                                                                                                      Content="НЕ" Style="{StaticResource FilterExcludeToggleButton}" Margin="3,0,6,0" ToolTip="Исключить из отбора (скрыть это значение)"/>
                                                                                        <TextBlock Grid.Column="2" Text="{Binding DisplayText}" Foreground="{StaticResource Vapor}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                                                                    </Grid>
                                                                                </DataTemplate>
                                                                            </ItemsControl.ItemTemplate>
                                                                        </ItemsControl>
                                                                    </ScrollViewer>
                                                                </Border>
                                                            </Grid>
                                                        </TabItem>

                                                        <TabItem Header="ПОТОК OS">
                                                            <Grid Margin="0,6,0,0">
                                                                <Grid.RowDefinitions>
                                                                    <RowDefinition Height="Auto"/>
                                                                    <RowDefinition Height="*"/>
                                                                </Grid.RowDefinitions>
                                                                <Grid Grid.Row="0" Margin="0,0,0,6">
                                                                    <Grid.ColumnDefinitions>
                                                                        <ColumnDefinition Width="*"/>
                                                                        <ColumnDefinition Width="Auto"/>
                                                                    </Grid.ColumnDefinitions>
                                                                    <TextBox x:Name="TxtTgSearchThreads" TextChanged="TxtTgSearchThreads_TextChanged" Padding="6,3" FontSize="11.5" ToolTip="Поиск потока OS..."/>
                                                                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="6,0,0,0">
                                                                        <Button Content="+ ВСЕ" Click="BtnTgSelectAllThreads_Click" Style="{StaticResource GhostButton}" Padding="5,2" FontSize="9.5" Margin="0,0,3,0" ToolTip="Включить все видимые"/>
                                                                        <Button Content="НЕ ВСЕ" Click="BtnTgExcludeAllThreads_Click" Style="{StaticResource GhostButton}" Padding="5,2" FontSize="9.5" Margin="0,0,3,0" ToolTip="Исключить все видимые"/>
                                                                        <Button Content="СНЯТЬ" Click="BtnTgClearThreads_Click" Style="{StaticResource GhostButton}" Padding="5,2" FontSize="9.5" ToolTip="Снять отметки"/>
                                                                    </StackPanel>
                                                                </Grid>
                                                                <Border Grid.Row="1" Background="{StaticResource CharcoalPress}" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1" CornerRadius="0">
                                                                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                                                        <ItemsControl x:Name="ListTgThreads" Margin="6,4">
                                                                            <ItemsControl.ItemTemplate>
                                                                                <DataTemplate>
                                                                                    <Grid Margin="1,2">
                                                                                        <Grid.ColumnDefinitions>
                                                                                            <ColumnDefinition Width="Auto"/>
                                                                                            <ColumnDefinition Width="Auto"/>
                                                                                            <ColumnDefinition Width="*"/>
                                                                                        </Grid.ColumnDefinitions>
                                                                                        <ToggleButton IsChecked="{Binding IsInclude, Mode=TwoWay}" Click="ChkTgFieldValueItem_Click"
                                                                                                      Content="ДА" Style="{StaticResource FilterIncludeToggleButton}" ToolTip="Включить в отбор (только это значение)"/>
                                                                                        <ToggleButton Grid.Column="1" IsChecked="{Binding IsExclude, Mode=TwoWay}" Click="ChkTgFieldValueItem_Click"
                                                                                                      Content="НЕ" Style="{StaticResource FilterExcludeToggleButton}" Margin="3,0,6,0" ToolTip="Исключить из отбора (скрыть это значение)"/>
                                                                                        <TextBlock Grid.Column="2" Text="{Binding DisplayText}" Foreground="{StaticResource Vapor}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                                                                    </Grid>
                                                                                </DataTemplate>
                                                                            </ItemsControl.ItemTemplate>
                                                                        </ItemsControl>
                                                                    </ScrollViewer>
                                                                </Border>
                                                            </Grid>
                                                        </TabItem>
                                                    </TabControl>
                                                </StackPanel>
                                            </Border>
                                        </Popup>
                                    </StackPanel>

                                    <!-- 6. Кнопка Очистки таблицы (рядом с кнопками фильтрации) -->
                                    <Button x:Name="BtnResetTgFilters" Grid.Column="6" Content="ОЧИСТИТЬ ТАБЛИЦУ" Click="BtnResetTgFilters_Click" Style="{StaticResource GhostButton}" VerticalAlignment="Center" Margin="0,0,6,0" ToolTip="Полностью очистить таблицу ТЖ и освободить оперативную память"/>

                                    <!-- 8. Счётчик записей (прижат к правому краю) -->
                                    <Border Grid.Column="8" Background="{StaticResource InkBlack}" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1" CornerRadius="15" Padding="10,3" VerticalAlignment="Center" HorizontalAlignment="Right">
                                        <TextBlock x:Name="TxtTgCountBadge" Text="0 записей" Foreground="{StaticResource EmberDiode}" FontWeight="SemiBold" FontSize="11" FontFamily="Consolas, monospace"/>
                                    </Border>
                                </Grid>

                                <!-- Панель активных фильтров ТЖ (кнопка раскрытия полного списка и сброса слева) -->
                                <StackPanel x:Name="ContainerTgFilterChips" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="0,8,0,0" Visibility="Collapsed">
                                    <ToggleButton x:Name="BtnTgMoreChips" Content="АКТИВНЫЕ ФИЛЬТРЫ (0) ▼" Style="{StaticResource GhostToggleButton}" Padding="8,4" FontSize="11" FontWeight="SemiBold" PreviewMouseLeftButtonDown="MenuToggleButton_PreviewMouseLeftButtonDown" Click="MenuToggleButton_Click"/>
                                    <Popup IsOpen="{Binding IsChecked, ElementName=BtnTgMoreChips}" StaysOpen="False" Closed="MenuPopup_Closed" PlacementTarget="{Binding ElementName=BtnTgMoreChips}" Placement="Bottom" AllowsTransparency="True">
                                        <Border Background="{StaticResource StudioSurface}" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1" CornerRadius="0" Padding="12" Margin="0,4,0,0" MaxWidth="640" MaxHeight="320">
                                            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                                <StackPanel>
                                                    <Grid Margin="0,0,0,8">
                                                        <TextBlock Text="ВСЕ АКТИВНЫЕ ФИЛЬТРЫ" FontWeight="SemiBold" Foreground="{StaticResource Vapor}" FontSize="11" VerticalAlignment="Center"/>
                                                        <Button HorizontalAlignment="Right" Content="СБРОСИТЬ ВСЕ" Click="BtnResetTgFilters_Click" Style="{StaticResource GhostButton}" Padding="6,2" FontSize="10"/>
                                                    </Grid>
                                                    <WrapPanel x:Name="PanelTgPopupChips"/>
                                                </StackPanel>
                                            </ScrollViewer>
                                        </Border>
                                    </Popup>
                                    <Button Content="СБРОСИТЬ ВСЕ" Click="BtnResetTgFilters_Click" Style="{StaticResource GhostButton}" Padding="8,4" FontSize="10.5" Margin="6,0,0,0" ToolTip="Сбросить все активные фильтры"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- Таблица ТЖ с аппаратной виртуализацией для мгновенного скролла 50 000+ строк -->
                        <DataGrid x:Name="GridTg" Grid.Row="2" VerticalAlignment="Stretch" AutoGenerateColumns="False" IsReadOnly="True" CanUserSortColumns="True" CanUserResizeColumns="True" Sorting="GridTg_Sorting" MouseDoubleClick="GridTg_MouseDoubleClick" MouseRightButtonDown="GridTg_MouseRightButtonDown" PreviewKeyDown="GridTg_PreviewKeyDown" Background="{StaticResource InkBlack}" RowBackground="#141414" AlternatingRowBackground="#1B1B1B" Foreground="{StaticResource PulpCream}" BorderThickness="1" BorderBrush="{StaticResource HairlineBorder}" HeadersVisibility="Column" RowHeaderWidth="0" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#242424" SelectionUnit="FullRow" SelectionMode="Extended" FocusVisualStyle="{x:Null}" ScrollViewer.CanContentScroll="True" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Auto" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" VirtualizingPanel.ScrollUnit="Pixel" EnableRowVirtualization="True" EnableColumnVirtualization="False">
                            <DataGrid.ContextMenu>
                                <ContextMenu Background="#1C1C1C" Foreground="{StaticResource PulpCream}" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1">
                                    <MenuItem Header="Скопировать значение ячейки" Click="TgMenuCopyCell_Click" InputGestureText="ПКМ"/>
                                    <MenuItem Header="Скопировать всю строку (текст)" Click="TgMenuCopyRowText_Click" InputGestureText="Ctrl+C"/>
                                    <MenuItem Header="Скопировать запись в JSON" Click="TgMenuCopyRowJson_Click"/>
                                    <MenuItem Header="Скопировать Контекст / SQL" Click="TgMenuCopyContextSql_Click"/>
                                    <Separator Background="{StaticResource HairlineBorder}"/>
                                    <MenuItem Header="Экспорт в Excel (.xlsx)" Click="TgMenuExportExcel_Click"/>
                                    <MenuItem Header="Открыть подробные детали" Click="TgMenuInspectRow_Click" FontWeight="SemiBold"/>
                                </ContextMenu>
                            </DataGrid.ContextMenu>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Дата и Время" Binding="{Binding DateFormatted}" SortMemberPath="Date" SortDirection="Descending" Width="180" MinWidth="140"/>
                                <DataGridTextColumn Header="Событие" Binding="{Binding Event}" SortMemberPath="Event" Width="110" MinWidth="70"/>
                                <DataGridTextColumn Binding="{Binding ExecutionStatus}" SortMemberPath="IsActiveOperation" Width="95" MinWidth="75">
                                    <DataGridTextColumn.Header>
                                        <TextBlock Text="Статус" ToolTip="Статус операции: 'Выполняется' (промежуточный срез незавершенной операции LONGDURATIONINFO) или 'Завершено' (окончание операции)"/>
                                    </DataGridTextColumn.Header>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Длительность" Binding="{Binding DurationFormatted}" SortMemberPath="DurationMs" Width="125" MinWidth="110"/>
                                <DataGridTextColumn Binding="{Binding ProcessName}" SortMemberPath="ProcessName" Width="90" MinWidth="70">
                                    <DataGridTextColumn.Header>
                                        <TextBlock Text="Процесс" ToolTip="Имя процесса кластера 1С: rphost (рабочий процесс), rmngr (менеджер кластера), ragent (агент сервера)"/>
                                    </DataGridTextColumn.Header>
                                </DataGridTextColumn>
                                <DataGridTextColumn Binding="{Binding ProcessId}" SortMemberPath="ProcessId" Width="65" MinWidth="50">
                                    <DataGridTextColumn.Header>
                                        <TextBlock Text="PID" ToolTip="PID (Process ID) — номер процесса службы 1С в Диспетчере задач Windows (например rphost_7916)"/>
                                    </DataGridTextColumn.Header>
                                </DataGridTextColumn>
                                <DataGridTextColumn Binding="{Binding Spid}" SortMemberPath="Spid" Width="65" MinWidth="50">
                                    <DataGridTextColumn.Header>
                                        <TextBlock Text="SPID" ToolTip="SPID (Server Process ID) — номер подключения к СУБД (MS SQL / PostgreSQL / dbpid). Связывает запись 1С с сессией в SSMS (sp_who2) или pg_stat_activity"/>
                                    </DataGridTextColumn.Header>
                                </DataGridTextColumn>
                                <DataGridTextColumn Binding="{Binding OSThread}" SortMemberPath="OSThread" Width="135" MinWidth="120">
                                    <DataGridTextColumn.Header>
                                        <TextBlock Text="Поток (OSThread)" ToolTip="Поток (OSThread) — уникальный номер потока ОС внутри процесса rphost. Позволяет связать длинную цепочку срезов LONGDURATIONINFO с завершающим событием"/>
                                    </DataGridTextColumn.Header>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Пользователь" Binding="{Binding User}" SortMemberPath="User" Width="140" MinWidth="90"/>
                                <DataGridTextColumn Header="Приложение" Binding="{Binding App}" SortMemberPath="App" Width="165" MinWidth="100"/>
                                <DataGridTextColumn Header="Контекст / SQL" Binding="{Binding Context}" SortMemberPath="Context" Width="*"/>
                                <DataGridTemplateColumn Header="Действия" Width="95" CanUserResize="False">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="ДЕТАЛИ" Style="{StaticResource InspectButton}" Click="BtnInspectRow_Click" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </TabItem>

                <!-- Вкладка Журнал Регистрации (ЖР) -->
                <TabItem Header="ЖУРНАЛ РЕГИСТРАЦИИ (ЖР)">
                    <Grid Margin="0,6,0,0" VerticalAlignment="Stretch">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Панель Загрузки и Парсинга ЖР -->
                        <Grid Grid.Row="0" Margin="0,0,0,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,6,0">
                                <TextBlock Text="КАТАЛОГ ЖР:" Foreground="{StaticResource MidAsh}" FontSize="11" FontWeight="SemiBold"/>
                                <ContentControl Style="{StaticResource InfoBadge}" ToolTip="Каталог информационной базы 1С с Журналом Регистрации (содержащий файл словаря 1Cv8.lgf и файлы событий *.lgp)."/>
                            </StackPanel>
                            <TextBox x:Name="TxtLgPath" Grid.Column="1" Text="C:\Logs\LGLogs" VerticalAlignment="Center"/>
                            <Button x:Name="BtnBrowseLg" Grid.Column="2" Content="ОБЗОР..." Click="BtnBrowseLg_Click" Margin="6,0,0,0"/>
                            <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,4,0">
                                <TextBlock Text="ЛИМИТ:" Foreground="{StaticResource MidAsh}" FontSize="11" FontWeight="SemiBold"/>
                                <ContentControl Style="{StaticResource InfoBadge}" ToolTip="Ограничение количества загружаемых строк Журнала Регистрации для моментальной диагностики."/>
                            </StackPanel>
                            <ComboBox x:Name="CmbLgLimit" Grid.Column="4" Width="110" VerticalAlignment="Center" SelectedIndex="5">
                                <ComboBoxItem Content="1 000"/>
                                <ComboBoxItem Content="2 000"/>
                                <ComboBoxItem Content="5 000"/>
                                <ComboBoxItem Content="10 000"/>
                                <ComboBoxItem Content="50 000"/>
                                <ComboBoxItem Content="Все записи"/>
                            </ComboBox>
                            <Button x:Name="BtnParseLg" Grid.Column="5" Content="РАСПАРСИТЬ ЖР" Click="BtnParseLg_Click" Style="{StaticResource PrimaryButton}" ToolTip="Запустить разбор словаря 1Cv8.lgf и параллельное чтение записей Журнала Регистрации (*.lgp)" Margin="6,0,0,0"/>
                            <Button x:Name="BtnCancelLg" Grid.Column="6" Content="ОСТАНОВИТЬ" Click="BtnCancelParsing_Click" Visibility="Collapsed" Background="#331414" Foreground="#FFFFFF" BorderBrush="#8A2B2B" BorderThickness="1" Padding="12,5" ToolTip="Остановить выполнение операции парсинга" Margin="6,0,0,0"/>
                            <Button x:Name="BtnExportLgJson" Grid.Column="7" Content="JSON" Click="BtnExportLgJson_Click" ToolTip="Экспорт выделенных строк (или всей таблицы) ЖР в JSON" Margin="6,0,0,0"/>
                            <Button x:Name="BtnExportLgExcel" Grid.Column="8" Content="EXCEL" Click="BtnExportLgExcel_Click" ToolTip="Экспорт таблицы ЖР в файл Microsoft Excel (.xlsx)" Margin="6,0,0,0"/>
                            <Button x:Name="BtnExportLgElastic" Grid.Column="9" Content="В ELASTIC" Click="BtnExportLgElastic_Click" ToolTip="Экспортировать текущие отображаемые записи таблицы напрямую в кластер Elasticsearch" Margin="6,0,0,0"/>
                        </Grid>

                        <!-- Панель Поиска, Фильтров и Сортировки ЖР -->
                        <Border Grid.Row="1" Background="{StaticResource CharcoalPress}" CornerRadius="0" Padding="10,8" Margin="0,0,0,8" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1">
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" MinWidth="180" MaxWidth="320"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- 1. Быстрый Поиск с поддержкой исключений '!' и '-' -->
                                    <Grid Grid.Column="0" Margin="0,0,8,0">
                                        <TextBox x:Name="TxtLgFilter" TextChanged="TxtLgFilter_TextChanged" Padding="8,6,8,6" FontSize="12" ToolTip="Быстрый поиск по всем полям. Для исключения используйте ! или - (например: !Сеанс, -Администратор, !Ошибка)"/>
                                    </Grid>

                                    <!-- 2. Фильтр по времени (С / По) -->
                                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                                        <TextBlock Text="ВРЕМЯ:" VerticalAlignment="Center" Foreground="{StaticResource MidAsh}" FontSize="11" FontWeight="SemiBold" Margin="0,0,4,0"/>
                                        <TextBlock Text="с" VerticalAlignment="Center" Foreground="{StaticResource MidAsh}" FontSize="11" Margin="0,0,2,0"/>
                                        <TextBox x:Name="TxtLgTimeFrom" TextChanged="TxtLgTimeFilter_TextChanged" Width="72" ToolTip="Время С (например 16:13:00, 16:13 или 2026-08-17 16:13)" FontSize="12" Padding="4,4"/>
                                        <TextBlock Text="по" VerticalAlignment="Center" Foreground="{StaticResource MidAsh}" FontSize="11" Margin="4,0,2,0"/>
                                        <TextBox x:Name="TxtLgTimeTo" TextChanged="TxtLgTimeFilter_TextChanged" Width="72" ToolTip="Время ПО (например 16:15:00 или 16:15)" FontSize="12" Padding="4,4"/>
                                    </StackPanel>

                                    <!-- 3. Основной фильтр событий/важности ЖР -->
                                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                                        <TextBlock Text="ФИЛЬТР:" VerticalAlignment="Center" Foreground="{StaticResource MidAsh}" FontSize="11" FontWeight="SemiBold" Margin="0,0,4,0"/>
                                        <ComboBox x:Name="CmbLgImportanceFilter" SelectionChanged="CmbLgImportanceFilter_SelectionChanged" Width="195" SelectedIndex="0">
                                            <ComboBoxItem Content="Все уровни и события"/>
                                            <ComboBoxItem Content="Ошибка"/>
                                            <ComboBoxItem Content="Предупреждение"/>
                                            <ComboBoxItem Content="Информация"/>
                                            <ComboBoxItem Content="Примечание"/>
                                        </ComboBox>
                                    </StackPanel>

                                    <!-- 4. Меню Включающих Параметров Фильтрации ЖР -->
                                    <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                                        <ToggleButton x:Name="BtnLgFilterMenu" Content="ДОП. ФИЛЬТРЫ ▼" Style="{StaticResource GhostToggleButton}" Padding="9,5" PreviewMouseLeftButtonDown="MenuToggleButton_PreviewMouseLeftButtonDown" Click="MenuToggleButton_Click"/>
                                        <Popup IsOpen="{Binding IsChecked, ElementName=BtnLgFilterMenu}" StaysOpen="False" Closed="MenuPopup_Closed" PlacementTarget="{Binding ElementName=BtnLgFilterMenu}" Placement="Bottom" AllowsTransparency="True">
                                            <Border Background="{StaticResource StudioSurface}" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1" CornerRadius="0" Padding="14" Margin="0,4,0,0">
                                                <StackPanel Width="360">
                                                    <TextBlock Text="ДОПОЛНИТЕЛЬНЫЕ ПАРАМЕТРЫ ФИЛЬТРАЦИИ ЖР" FontWeight="SemiBold" Foreground="{StaticResource Vapor}" FontSize="12" Margin="0,0,0,10"/>

                                                    <TextBlock Text="ВКЛЮЧАЮЩИЕ УРОВНИ ВАЖНОСТИ:" Foreground="{StaticResource MidAsh}" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                                    <WrapPanel Margin="0,0,0,10">
                                                        <CheckBox x:Name="ChkLgIncError" Content="Ошибка" Foreground="{StaticResource Vapor}" Margin="0,0,12,4" Click="ChkLgFilter_Click"/>
                                                        <CheckBox x:Name="ChkLgIncWarn" Content="Предупреждение" Foreground="{StaticResource Vapor}" Margin="0,0,12,4" Click="ChkLgFilter_Click"/>
                                                        <CheckBox x:Name="ChkLgIncInfo" Content="Информация" Foreground="{StaticResource Vapor}" Margin="0,0,12,4" Click="ChkLgFilter_Click"/>
                                                        <CheckBox x:Name="ChkLgIncNote" Content="Примечание" Foreground="{StaticResource Vapor}" Margin="0,0,0,4" Click="ChkLgFilter_Click"/>
                                                    </WrapPanel>

                                                    <TextBlock Text="СОРТИРОВКА ТАБЛИЦЫ:" Foreground="{StaticResource MidAsh}" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                                    <ComboBox x:Name="CmbLgSortPreset" SelectionChanged="CmbLgSortPreset_SelectionChanged" SelectedIndex="0">
                                                        <ComboBoxItem Content="Дата (сначала новые)"/>
                                                        <ComboBoxItem Content="Дата (сначала старые)"/>
                                                        <ComboBoxItem Content="Важность + Дата"/>
                                                        <ComboBoxItem Content="Событие + Дата"/>
                                                        <ComboBoxItem Content="Пользователь + Дата"/>
                                                    </ComboBox>
                                                </StackPanel>
                                            </Border>
                                        </Popup>
                                    </StackPanel>

                                    <!-- 5. Меню Исключающих Фильтров ЖР (НЕ) с множественным выбором -->
                                    <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                                        <ToggleButton x:Name="BtnLgExcludeMenu" Content="ИСКЛЮЧЕНИЯ (НЕ) ▼" Style="{StaticResource GhostToggleButton}" Padding="9,5" PreviewMouseLeftButtonDown="MenuToggleButton_PreviewMouseLeftButtonDown" Click="MenuToggleButton_Click"/>
                                        <Popup IsOpen="{Binding IsChecked, ElementName=BtnLgExcludeMenu}" StaysOpen="False" Closed="MenuPopup_Closed" PlacementTarget="{Binding ElementName=BtnLgExcludeMenu}" Placement="Bottom" AllowsTransparency="True">
                                            <Border Background="{StaticResource StudioSurface}" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1" CornerRadius="0" Padding="14" Margin="0,4,0,0">
                                                <StackPanel Width="420">
                                                    <TextBlock Text="ИСКЛЮЧЕНИЕ ДАННЫХ ИЗ ЖР (ФИЛЬТРЫ НЕ)" FontWeight="SemiBold" Foreground="{StaticResource Vapor}" FontSize="12" Margin="0,0,0,10"/>

                                                    <!-- Исключение уровней важности -->
                                                    <TextBlock Text="ИСКЛЮЧИТЬ УРОВНИ ВАЖНОСТИ:" Foreground="{StaticResource MidAsh}" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                                    <WrapPanel Margin="0,0,0,10">
                                                        <CheckBox x:Name="ChkLgExError" Content="Ошибки" Foreground="{StaticResource Vapor}" Margin="0,0,12,4" Click="ChkLgFilter_Click"/>
                                                        <CheckBox x:Name="ChkLgExWarn" Content="Предупреждения" Foreground="{StaticResource Vapor}" Margin="0,0,12,4" Click="ChkLgFilter_Click"/>
                                                        <CheckBox x:Name="ChkLgExInfo" Content="Информацию" Foreground="{StaticResource Vapor}" Margin="0,0,12,4" Click="ChkLgFilter_Click"/>
                                                        <CheckBox x:Name="ChkLgExNote" Content="Примечания" Foreground="{StaticResource Vapor}" Margin="0,0,0,4" Click="ChkLgFilter_Click"/>
                                                    </WrapPanel>

                                                    <!-- Шапка секции событий ЖР с поиском и кнопками -->
                                                    <Grid Margin="0,0,0,6">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Text="Исключить события ЖР:" VerticalAlignment="Center" Foreground="{StaticResource MidAsh}" FontSize="11" FontWeight="SemiBold"/>
                                                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                                                            <Button Content="СНЯТЬ ВСЕ" Click="BtnLgExcludeClearAll_Click" Style="{StaticResource GhostButton}" Padding="6,2" FontSize="10"/>
                                                        </StackPanel>
                                                    </Grid>

                                                    <TextBox x:Name="TxtLgExcludeEventSearch" TextChanged="TxtLgExcludeEventSearch_TextChanged" Padding="8,4" Margin="0,0,0,6" FontSize="11.5" ToolTip="Фильтр по списку событий..."/>

                                                    <!-- Скроллируемый список событий ЖР с чекбоксами -->
                                                    <Border Background="{StaticResource CharcoalPress}" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1" CornerRadius="0" Height="180" Margin="0,0,0,10">
                                                        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                                            <ItemsControl x:Name="ListLgExcludeEvents" Margin="6,4">
                                                                <ItemsControl.ItemTemplate>
                                                                    <DataTemplate>
                                                                        <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}" Margin="2,3" Click="ChkLgExcludeEventItem_Click">
                                                                            <CheckBox.Content>
                                                                                <TextBlock TextWrapping="NoWrap" TextTrimming="CharacterEllipsis">
                                                                                    <Run Text="{Binding Title}" FontWeight="SemiBold" Foreground="{StaticResource Vapor}"/>
                                                                                    <Run Text=" — " Foreground="{StaticResource MidAsh}"/>
                                                                                    <Run Text="{Binding Subtitle}" Foreground="{StaticResource MidAsh}" FontSize="11"/>
                                                                                </TextBlock>
                                                                            </CheckBox.Content>
                                                                        </CheckBox>
                                                                    </DataTemplate>
                                                                </ItemsControl.ItemTemplate>
                                                            </ItemsControl>
                                                        </ScrollViewer>
                                                    </Border>

                                                    <!-- Памятка по строке поиска -->
                                                    <Border Background="#141414" CornerRadius="0" Padding="8,6" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1">
                                                        <TextBlock Foreground="{StaticResource MidAsh}" FontSize="11" TextWrapping="Wrap" LineHeight="15">
                                                            <Run FontWeight="SemiBold" Foreground="{StaticResource Vapor}">РУЧНЫЕ ИСКЛЮЧЕНИЯ:</Run> вводите в поиск <Run FontWeight="Bold" Foreground="{StaticResource EmberDiode}">!</Run> перед словом (например <Run Foreground="{StaticResource Vapor}">!Сеанс</Run>, <Run Foreground="{StaticResource Vapor}">!Администратор</Run>, <Run Foreground="{StaticResource Vapor}">!Информация</Run>).
                                                        </TextBlock>
                                                    </Border>
                                                </StackPanel>
                                            </Border>
                                        </Popup>
                                    </StackPanel>

                                    <!-- 6. Меню Отбора по полям ЖР (Пользователи, Приложения, Метаданные, События) -->
                                    <StackPanel Grid.Column="5" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                                        <ToggleButton x:Name="BtnLgFieldsMenu" Content="ОТБОР ПО ПОЛЯМ ▼" Style="{StaticResource GhostToggleButton}" Padding="9,5" PreviewMouseLeftButtonDown="MenuToggleButton_PreviewMouseLeftButtonDown" Click="MenuToggleButton_Click"/>
                                        <Popup IsOpen="{Binding IsChecked, ElementName=BtnLgFieldsMenu}" StaysOpen="False" Closed="MenuPopup_Closed" PlacementTarget="{Binding ElementName=BtnLgFieldsMenu}" Placement="Bottom" AllowsTransparency="True">
                                            <Border Background="{StaticResource StudioSurface}" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1" CornerRadius="0" Padding="14" Margin="0,4,0,0">
                                                <StackPanel Width="560">
                                                    <Grid Margin="0,0,0,8">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Text="ОТБОР ЗАПИСЕЙ ЖР ПО ПОЛЯМ" FontWeight="SemiBold" Foreground="{StaticResource Vapor}" FontSize="12" VerticalAlignment="Center"/>
                                                        <Button Grid.Column="1" Content="СБРОСИТЬ ВСЕ" Click="BtnLgClearAllFieldFilters_Click" Style="{StaticResource GhostButton}" Padding="6,2" FontSize="10"/>
                                                    </Grid>

                                                    <TabControl Background="Transparent" BorderThickness="0" Height="270">
                                                        <TabItem Header="ПОЛЬЗОВАТЕЛИ">
                                                            <Grid Margin="0,6,0,0">
                                                                <Grid.RowDefinitions>
                                                                    <RowDefinition Height="Auto"/>
                                                                    <RowDefinition Height="*"/>
                                                                </Grid.RowDefinitions>
                                                                <Grid Grid.Row="0" Margin="0,0,0,6">
                                                                    <Grid.ColumnDefinitions>
                                                                        <ColumnDefinition Width="*"/>
                                                                        <ColumnDefinition Width="Auto"/>
                                                                    </Grid.ColumnDefinitions>
                                                                    <TextBox x:Name="TxtLgSearchUsers" TextChanged="TxtLgSearchUsers_TextChanged" Padding="6,3" FontSize="11.5" ToolTip="Поиск пользователя..."/>
                                                                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="6,0,0,0">
                                                                        <Button Content="+ ВСЕ" Click="BtnLgSelectAllUsers_Click" Style="{StaticResource GhostButton}" Padding="5,2" FontSize="9.5" Margin="0,0,3,0" ToolTip="Включить все видимые"/>
                                                                        <Button Content="НЕ ВСЕ" Click="BtnLgExcludeAllUsers_Click" Style="{StaticResource GhostButton}" Padding="5,2" FontSize="9.5" Margin="0,0,3,0" ToolTip="Исключить все видимые"/>
                                                                        <Button Content="СНЯТЬ" Click="BtnLgClearUsers_Click" Style="{StaticResource GhostButton}" Padding="5,2" FontSize="9.5" ToolTip="Снять отметки"/>
                                                                    </StackPanel>
                                                                </Grid>
                                                                <Border Grid.Row="1" Background="{StaticResource CharcoalPress}" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1" CornerRadius="0">
                                                                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                                                        <ItemsControl x:Name="ListLgUsers" Margin="6,4">
                                                                            <ItemsControl.ItemTemplate>
                                                                                <DataTemplate>
                                                                                    <Grid Margin="1,2">
                                                                                        <Grid.ColumnDefinitions>
                                                                                            <ColumnDefinition Width="Auto"/>
                                                                                            <ColumnDefinition Width="Auto"/>
                                                                                            <ColumnDefinition Width="*"/>
                                                                                        </Grid.ColumnDefinitions>
                                                                                        <ToggleButton IsChecked="{Binding IsInclude, Mode=TwoWay}" Click="ChkLgFieldValueItem_Click"
                                                                                                      Content="ДА" Style="{StaticResource FilterIncludeToggleButton}" ToolTip="Включить в отбор (только это значение)"/>
                                                                                        <ToggleButton Grid.Column="1" IsChecked="{Binding IsExclude, Mode=TwoWay}" Click="ChkLgFieldValueItem_Click"
                                                                                                      Content="НЕ" Style="{StaticResource FilterExcludeToggleButton}" Margin="3,0,6,0" ToolTip="Исключить из отбора (скрыть это значение)"/>
                                                                                        <TextBlock Grid.Column="2" Text="{Binding DisplayText}" Foreground="{StaticResource Vapor}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                                                                    </Grid>
                                                                                </DataTemplate>
                                                                            </ItemsControl.ItemTemplate>
                                                                        </ItemsControl>
                                                                    </ScrollViewer>
                                                                </Border>
                                                            </Grid>
                                                        </TabItem>

                                                        <TabItem Header="ПРИЛОЖЕНИЯ">
                                                            <Grid Margin="0,6,0,0">
                                                                <Grid.RowDefinitions>
                                                                    <RowDefinition Height="Auto"/>
                                                                    <RowDefinition Height="*"/>
                                                                </Grid.RowDefinitions>
                                                                <Grid Grid.Row="0" Margin="0,0,0,6">
                                                                    <Grid.ColumnDefinitions>
                                                                        <ColumnDefinition Width="*"/>
                                                                        <ColumnDefinition Width="Auto"/>
                                                                    </Grid.ColumnDefinitions>
                                                                    <TextBox x:Name="TxtLgSearchApps" TextChanged="TxtLgSearchApps_TextChanged" Padding="6,3" FontSize="11.5" ToolTip="Поиск приложения..."/>
                                                                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="6,0,0,0">
                                                                        <Button Content="+ ВСЕ" Click="BtnLgSelectAllApps_Click" Style="{StaticResource GhostButton}" Padding="5,2" FontSize="9.5" Margin="0,0,3,0" ToolTip="Включить все видимые"/>
                                                                        <Button Content="НЕ ВСЕ" Click="BtnLgExcludeAllApps_Click" Style="{StaticResource GhostButton}" Padding="5,2" FontSize="9.5" Margin="0,0,3,0" ToolTip="Исключить все видимые"/>
                                                                        <Button Content="СНЯТЬ" Click="BtnLgClearApps_Click" Style="{StaticResource GhostButton}" Padding="5,2" FontSize="9.5" ToolTip="Снять отметки"/>
                                                                    </StackPanel>
                                                                </Grid>
                                                                <Border Grid.Row="1" Background="{StaticResource CharcoalPress}" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1" CornerRadius="0">
                                                                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                                                        <ItemsControl x:Name="ListLgApps" Margin="6,4">
                                                                            <ItemsControl.ItemTemplate>
                                                                                <DataTemplate>
                                                                                    <Grid Margin="1,2">
                                                                                        <Grid.ColumnDefinitions>
                                                                                            <ColumnDefinition Width="Auto"/>
                                                                                            <ColumnDefinition Width="Auto"/>
                                                                                            <ColumnDefinition Width="*"/>
                                                                                        </Grid.ColumnDefinitions>
                                                                                        <ToggleButton IsChecked="{Binding IsInclude, Mode=TwoWay}" Click="ChkLgFieldValueItem_Click"
                                                                                                      Content="ДА" Style="{StaticResource FilterIncludeToggleButton}" ToolTip="Включить в отбор (только это значение)"/>
                                                                                        <ToggleButton Grid.Column="1" IsChecked="{Binding IsExclude, Mode=TwoWay}" Click="ChkLgFieldValueItem_Click"
                                                                                                      Content="НЕ" Style="{StaticResource FilterExcludeToggleButton}" Margin="3,0,6,0" ToolTip="Исключить из отбора (скрыть это значение)"/>
                                                                                        <TextBlock Grid.Column="2" Text="{Binding DisplayText}" Foreground="{StaticResource Vapor}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                                                                    </Grid>
                                                                                </DataTemplate>
                                                                            </ItemsControl.ItemTemplate>
                                                                        </ItemsControl>
                                                                    </ScrollViewer>
                                                                </Border>
                                                            </Grid>
                                                        </TabItem>

                                                        <TabItem Header="МЕТАДАННЫЕ">
                                                            <Grid Margin="0,6,0,0">
                                                                <Grid.RowDefinitions>
                                                                    <RowDefinition Height="Auto"/>
                                                                    <RowDefinition Height="*"/>
                                                                </Grid.RowDefinitions>
                                                                <Grid Grid.Row="0" Margin="0,0,0,6">
                                                                    <Grid.ColumnDefinitions>
                                                                        <ColumnDefinition Width="*"/>
                                                                        <ColumnDefinition Width="Auto"/>
                                                                    </Grid.ColumnDefinitions>
                                                                    <TextBox x:Name="TxtLgSearchMetas" TextChanged="TxtLgSearchMetas_TextChanged" Padding="6,3" FontSize="11.5" ToolTip="Поиск метаданных..."/>
                                                                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="6,0,0,0">
                                                                        <Button Content="+ ВСЕ" Click="BtnLgSelectAllMetas_Click" Style="{StaticResource GhostButton}" Padding="5,2" FontSize="9.5" Margin="0,0,3,0" ToolTip="Включить все видимые"/>
                                                                        <Button Content="НЕ ВСЕ" Click="BtnLgExcludeAllMetas_Click" Style="{StaticResource GhostButton}" Padding="5,2" FontSize="9.5" Margin="0,0,3,0" ToolTip="Исключить все видимые"/>
                                                                        <Button Content="СНЯТЬ" Click="BtnLgClearMetas_Click" Style="{StaticResource GhostButton}" Padding="5,2" FontSize="9.5" ToolTip="Снять отметки"/>
                                                                    </StackPanel>
                                                                </Grid>
                                                                <Border Grid.Row="1" Background="{StaticResource CharcoalPress}" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1" CornerRadius="0">
                                                                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                                                        <ItemsControl x:Name="ListLgMetas" Margin="6,4">
                                                                            <ItemsControl.ItemTemplate>
                                                                                <DataTemplate>
                                                                                    <Grid Margin="1,2">
                                                                                        <Grid.ColumnDefinitions>
                                                                                            <ColumnDefinition Width="Auto"/>
                                                                                            <ColumnDefinition Width="Auto"/>
                                                                                            <ColumnDefinition Width="*"/>
                                                                                        </Grid.ColumnDefinitions>
                                                                                        <ToggleButton IsChecked="{Binding IsInclude, Mode=TwoWay}" Click="ChkLgFieldValueItem_Click"
                                                                                                      Content="ДА" Style="{StaticResource FilterIncludeToggleButton}" ToolTip="Включить в отбор (только это значение)"/>
                                                                                        <ToggleButton Grid.Column="1" IsChecked="{Binding IsExclude, Mode=TwoWay}" Click="ChkLgFieldValueItem_Click"
                                                                                                      Content="НЕ" Style="{StaticResource FilterExcludeToggleButton}" Margin="3,0,6,0" ToolTip="Исключить из отбора (скрыть это значение)"/>
                                                                                        <TextBlock Grid.Column="2" Text="{Binding DisplayText}" Foreground="{StaticResource Vapor}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                                                                    </Grid>
                                                                                </DataTemplate>
                                                                            </ItemsControl.ItemTemplate>
                                                                        </ItemsControl>
                                                                    </ScrollViewer>
                                                                </Border>
                                                            </Grid>
                                                        </TabItem>

                                                        <TabItem Header="СОБЫТИЯ">
                                                            <Grid Margin="0,6,0,0">
                                                                <Grid.RowDefinitions>
                                                                    <RowDefinition Height="Auto"/>
                                                                    <RowDefinition Height="*"/>
                                                                </Grid.RowDefinitions>
                                                                <Grid Grid.Row="0" Margin="0,0,0,6">
                                                                    <Grid.ColumnDefinitions>
                                                                        <ColumnDefinition Width="*"/>
                                                                        <ColumnDefinition Width="Auto"/>
                                                                    </Grid.ColumnDefinitions>
                                                                    <TextBox x:Name="TxtLgSearchEvents" TextChanged="TxtLgSearchEvents_TextChanged" Padding="6,3" FontSize="11.5" ToolTip="Поиск события ЖР..."/>
                                                                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="6,0,0,0">
                                                                        <Button Content="+ ВСЕ" Click="BtnLgSelectAllEvents_Click" Style="{StaticResource GhostButton}" Padding="5,2" FontSize="9.5" Margin="0,0,3,0" ToolTip="Включить все видимые"/>
                                                                        <Button Content="НЕ ВСЕ" Click="BtnLgExcludeAllEvents_Click" Style="{StaticResource GhostButton}" Padding="5,2" FontSize="9.5" Margin="0,0,3,0" ToolTip="Исключить все видимые"/>
                                                                        <Button Content="СНЯТЬ" Click="BtnLgClearEvents_Click" Style="{StaticResource GhostButton}" Padding="5,2" FontSize="9.5" ToolTip="Снять отметки"/>
                                                                    </StackPanel>
                                                                </Grid>
                                                                <Border Grid.Row="1" Background="{StaticResource CharcoalPress}" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1" CornerRadius="0">
                                                                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                                                        <ItemsControl x:Name="ListLgEvents" Margin="6,4">
                                                                            <ItemsControl.ItemTemplate>
                                                                                <DataTemplate>
                                                                                    <Grid Margin="1,2">
                                                                                        <Grid.ColumnDefinitions>
                                                                                            <ColumnDefinition Width="Auto"/>
                                                                                            <ColumnDefinition Width="Auto"/>
                                                                                            <ColumnDefinition Width="*"/>
                                                                                        </Grid.ColumnDefinitions>
                                                                                        <ToggleButton IsChecked="{Binding IsInclude, Mode=TwoWay}" Click="ChkLgFieldValueItem_Click"
                                                                                                      Content="ДА" Style="{StaticResource FilterIncludeToggleButton}" ToolTip="Включить в отбор (только это значение)"/>
                                                                                        <ToggleButton Grid.Column="1" IsChecked="{Binding IsExclude, Mode=TwoWay}" Click="ChkLgFieldValueItem_Click"
                                                                                                      Content="НЕ" Style="{StaticResource FilterExcludeToggleButton}" Margin="3,0,6,0" ToolTip="Исключить из отбора (скрыть это значение)"/>
                                                                                        <TextBlock Grid.Column="2" Text="{Binding DisplayText}" Foreground="{StaticResource Vapor}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                                                                    </Grid>
                                                                                </DataTemplate>
                                                                            </ItemsControl.ItemTemplate>
                                                                        </ItemsControl>
                                                                    </ScrollViewer>
                                                                </Border>
                                                            </Grid>
                                                        </TabItem>
                                                    </TabControl>
                                                </StackPanel>
                                            </Border>
                                        </Popup>
                                    </StackPanel>

                                    <!-- 6. Кнопка Очистки таблицы (рядом с кнопками фильтрации) -->
                                    <Button x:Name="BtnResetLgFilters" Grid.Column="6" Content="ОЧИСТИТЬ ТАБЛИЦУ" Click="BtnResetLgFilters_Click" Style="{StaticResource GhostButton}" VerticalAlignment="Center" Margin="0,0,6,0" ToolTip="Полностью очистить таблицу ЖР и освободить оперативную память"/>

                                    <!-- 8. Счётчик записей (прижат к правому краю) -->
                                    <Border Grid.Column="8" Background="{StaticResource InkBlack}" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1" CornerRadius="15" Padding="10,3" VerticalAlignment="Center" HorizontalAlignment="Right">
                                        <TextBlock x:Name="TxtLgCountBadge" Text="0 записей" Foreground="{StaticResource EmberDiode}" FontWeight="SemiBold" FontSize="11" FontFamily="Consolas, monospace"/>
                                    </Border>
                                </Grid>

                                <!-- Панель активных фильтров ЖР (кнопка раскрытия полного списка и сброса слева) -->
                                <StackPanel x:Name="ContainerLgFilterChips" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="0,8,0,0" Visibility="Collapsed">
                                    <ToggleButton x:Name="BtnLgMoreChips" Content="АКТИВНЫЕ ФИЛЬТРЫ (0) ▼" Style="{StaticResource GhostToggleButton}" Padding="8,4" FontSize="11" FontWeight="SemiBold" PreviewMouseLeftButtonDown="MenuToggleButton_PreviewMouseLeftButtonDown" Click="MenuToggleButton_Click"/>
                                    <Popup IsOpen="{Binding IsChecked, ElementName=BtnLgMoreChips}" StaysOpen="False" Closed="MenuPopup_Closed" PlacementTarget="{Binding ElementName=BtnLgMoreChips}" Placement="Bottom" AllowsTransparency="True">
                                        <Border Background="{StaticResource StudioSurface}" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1" CornerRadius="0" Padding="12" Margin="0,4,0,0" MaxWidth="640" MaxHeight="320">
                                            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                                <StackPanel>
                                                    <Grid Margin="0,0,0,8">
                                                        <TextBlock Text="ВСЕ АКТИВНЫЕ ФИЛЬТРЫ" FontWeight="SemiBold" Foreground="{StaticResource Vapor}" FontSize="11" VerticalAlignment="Center"/>
                                                        <Button HorizontalAlignment="Right" Content="СБРОСИТЬ ВСЕ" Click="BtnResetLgFilters_Click" Style="{StaticResource GhostButton}" Padding="6,2" FontSize="10"/>
                                                    </Grid>
                                                    <WrapPanel x:Name="PanelLgPopupChips"/>
                                                </StackPanel>
                                            </ScrollViewer>
                                        </Border>
                                    </Popup>
                                    <Button Content="СБРОСИТЬ ВСЕ" Click="BtnResetLgFilters_Click" Style="{StaticResource GhostButton}" Padding="8,4" FontSize="10.5" Margin="6,0,0,0" ToolTip="Сбросить все активные фильтры"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- Таблица ЖР с аппаратной виртуализацией для мгновенного скролла 50 000+ строк -->
                        <DataGrid x:Name="GridLg" Grid.Row="2" VerticalAlignment="Stretch" AutoGenerateColumns="False" IsReadOnly="True" CanUserSortColumns="True" CanUserResizeColumns="True" Sorting="GridLg_Sorting" MouseDoubleClick="GridLg_MouseDoubleClick" MouseRightButtonDown="GridLg_MouseRightButtonDown" PreviewKeyDown="GridLg_PreviewKeyDown" Background="{StaticResource InkBlack}" RowBackground="#141414" AlternatingRowBackground="#1B1B1B" Foreground="{StaticResource PulpCream}" BorderThickness="1" BorderBrush="{StaticResource HairlineBorder}" HeadersVisibility="Column" RowHeaderWidth="0" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#242424" SelectionUnit="FullRow" SelectionMode="Extended" FocusVisualStyle="{x:Null}" ScrollViewer.CanContentScroll="True" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Auto" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" VirtualizingPanel.ScrollUnit="Pixel" EnableRowVirtualization="True" EnableColumnVirtualization="False">
                            <DataGrid.ContextMenu>
                                <ContextMenu Background="#1C1C1C" Foreground="{StaticResource PulpCream}" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1">
                                    <MenuItem Header="Скопировать значение ячейки" Click="LgMenuCopyCell_Click" InputGestureText="ПКМ"/>
                                    <MenuItem Header="Скопировать всю строку (текст)" Click="LgMenuCopyRowText_Click" InputGestureText="Ctrl+C"/>
                                    <MenuItem Header="Скопировать запись в JSON" Click="LgMenuCopyRowJson_Click"/>
                                    <MenuItem Header="Скопировать Комментарий" Click="LgMenuCopyComment_Click"/>
                                    <Separator Background="{StaticResource HairlineBorder}"/>
                                    <MenuItem Header="Экспорт в Excel (.xlsx)" Click="LgMenuExportExcel_Click"/>
                                    <MenuItem Header="Открыть подробные детали" Click="LgMenuInspectRow_Click" FontWeight="SemiBold"/>
                                </ContextMenu>
                            </DataGrid.ContextMenu>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Дата и Время" Binding="{Binding DateFormatted}" SortMemberPath="Date" SortDirection="Descending" Width="180" MinWidth="140"/>
                                <DataGridTextColumn Header="Событие" Binding="{Binding Event}" SortMemberPath="Event" Width="150" MinWidth="110"/>
                                <DataGridTextColumn Header="Важность" Binding="{Binding Importance}" SortMemberPath="Importance" Width="105" MinWidth="85"/>
                                <DataGridTextColumn Header="Пользователь" Binding="{Binding User}" SortMemberPath="User" Width="140" MinWidth="90"/>
                                <DataGridTextColumn Header="Приложение" Binding="{Binding App}" SortMemberPath="App" Width="165" MinWidth="100"/>
                                <DataGridTextColumn Header="Метаданные" Binding="{Binding Meta}" SortMemberPath="Meta" Width="165" MinWidth="110"/>
                                <DataGridTextColumn Header="Комментарий" Binding="{Binding Comment}" SortMemberPath="Comment" Width="*"/>
                                <DataGridTemplateColumn Header="Действия" Width="95" CanUserResize="False">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="ДЕТАЛИ" Style="{StaticResource InspectButton}" Click="BtnInspectRow_Click" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </TabItem>

                <!-- Вкладка Настройки и Авторизация -->
                <TabItem Header="КОНФИГУРАЦИЯ ПОДКЛЮЧЕНИЯ (ELASTIC &amp; KIBANA)">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,6,0,0">
                        <StackPanel>
                            <!-- Настройки подключения Elasticsearch & Kibana -->
                            <Border Background="{StaticResource CharcoalPress}" CornerRadius="0" Padding="14" Margin="0,0,0,10" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1">
                                <StackPanel>
                                    <TextBlock Text="ПОДКЛЮЧЕНИЕ И ИНТЕГРАЦИЯ С ELASTICSEARCH &amp; KIBANA" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource Vapor}" Margin="0,0,0,10"/>
                                    
                                    <Grid Margin="0,0,0,6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="220"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <TextBlock Text="Адрес Elasticsearch (ServerUrl):" Foreground="{StaticResource MidAsh}" FontSize="12"/>
                                            <ContentControl Style="{StaticResource InfoBadge}" ToolTip="Сетевой адрес и порт ноды Elasticsearch (например: http://localhost:9200 или https://elastic.corp:9200). Используется для потоковой отправки логов и аналитики."/>
                                        </StackPanel>
                                        <TextBox x:Name="TxtElasticUrl" Grid.Column="1" Text="http://localhost:9200"/>
                                        <Button x:Name="BtnTestElastic" Grid.Column="2" Content="ПРОВЕРИТЬ СВЯЗЬ" Click="BtnTestElastic_Click" Margin="6,0,0,0"/>
                                    </Grid>

                                    <Grid Margin="0,0,0,6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="220"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="Пользователь (Username):" VerticalAlignment="Center" Foreground="{StaticResource MidAsh}" FontSize="12"/>
                                        <TextBox x:Name="TxtElasticUser" Grid.Column="1" Text="elastic"/>
                                    </Grid>

                                    <Grid Margin="0,0,0,6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="220"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="Пароль (Password):" VerticalAlignment="Center" Foreground="{StaticResource MidAsh}" FontSize="12"/>
                                        <PasswordBox x:Name="TxtElasticPass" Grid.Column="1"/>
                                    </Grid>

                                    <Grid Margin="0,0,0,6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="220"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <TextBlock Text="API Ключ (ApiKey):" Foreground="{StaticResource MidAsh}" FontSize="12"/>
                                            <ContentControl Style="{StaticResource InfoBadge}" ToolTip="Альтернативный способ аутентификации в Elasticsearch (Base64 токен). Если указан, логин и пароль не требуются."/>
                                        </StackPanel>
                                        <TextBox x:Name="TxtElasticApiKey" Grid.Column="1"/>
                                    </Grid>

                                    <Grid Margin="0,0,0,6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="220"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <TextBlock Text="Адрес Kibana (ServerUrl):" Foreground="{StaticResource MidAsh}" FontSize="12"/>
                                            <ContentControl Style="{StaticResource InfoBadge}" ToolTip="Адрес веб-интерфейса Kibana (по умолчанию http://localhost:5601). Используется для проверки доступности и создания дашбордов."/>
                                        </StackPanel>
                                        <TextBox x:Name="TxtKibanaUrl" Grid.Column="1" Text="http://localhost:5601"/>
                                        <Button x:Name="BtnTestKibana" Grid.Column="2" Content="ПРОВЕРИТЬ KIBANA" Click="BtnTestKibana_Click" Margin="6,0,0,0"/>
                                    </Grid>

                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="220,4,0,0">
                                        <CheckBox x:Name="ChkElasticEnabled" Content="Включить публикацию в Elasticsearch при мониторинге" Foreground="{StaticResource PulpCream}" IsChecked="True" FontSize="12"/>
                                        <ContentControl Style="{StaticResource InfoBadge}" ToolTip="При включении фоновая служба будет автоматически отправлять новые разобранные записи в Elasticsearch в реальном времени."/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <!-- Префиксы индексов и локальный дамп -->
                            <Border Background="{StaticResource CharcoalPress}" CornerRadius="0" Padding="14" Margin="0,0,0,10" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1">
                                <StackPanel>
                                    <TextBlock Text="ИНДЕКСЫ И ПАРАМЕТРЫ СОХРАНЕНИЯ" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource Vapor}" Margin="0,0,0,10"/>

                                    <Grid Margin="0,0,0,6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="220"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <TextBlock Text="Префикс индекса ТЖ:" Foreground="{StaticResource MidAsh}" FontSize="12"/>
                                            <ContentControl Style="{StaticResource InfoBadge}" ToolTip="Базовое имя суточных индексов для Технологического Журнала (например: techlog -> 'techlog-2026.08.21')."/>
                                        </StackPanel>
                                        <TextBox x:Name="TxtTechLogPrefix" Grid.Column="1" Text="techlog"/>
                                    </Grid>

                                    <Grid Margin="0,0,0,6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="220"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <TextBlock Text="Префикс индекса ЖР:" Foreground="{StaticResource MidAsh}" FontSize="12"/>
                                            <ContentControl Style="{StaticResource InfoBadge}" ToolTip="Базовое имя суточных индексов для Журнала Регистрации (например: events -> 'events-2026.08.21')."/>
                                        </StackPanel>
                                        <TextBox x:Name="TxtEventLogPrefix" Grid.Column="1" Text="events"/>
                                    </Grid>

                                    <Grid Margin="0,0,0,6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="220"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <TextBlock Text="Каталог локального JSON дампа:" Foreground="{StaticResource MidAsh}" FontSize="12"/>
                                            <ContentControl Style="{StaticResource InfoBadge}" ToolTip="Путь к локальной папке для автоматического сохранения резервных копий распарсенных событий в формате NDJSON без обращения к сети."/>
                                        </StackPanel>
                                        <TextBox x:Name="TxtDumpDirectory" Grid.Column="1" Text="parsed_logs"/>
                                    </Grid>

                                    <Grid Margin="0,0,0,6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="220"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <TextBlock Text="Макс. объем полей лога (байты):" Foreground="{StaticResource MidAsh}" FontSize="12"/>
                                            <ContentControl Style="{StaticResource InfoBadge}" ToolTip="Максимальный размер одного текстового поля (SQL-запроса, стека ошибки). Предотвращает переполнение памяти при аномально больших событиях (по умолчанию 65536 байт = 64 КБ)."/>
                                        </StackPanel>
                                        <TextBox x:Name="TxtMaxFieldLength" Grid.Column="1" Text="65536"/>
                                    </Grid>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>
            </TabControl>

            <!-- Компактный Статус Бар (Superlative Industrial Spec Footer) -->
            <Border Grid.Row="2" Background="{StaticResource CharcoalPress}" CornerRadius="0" Padding="12,6" Margin="0,6,0,0" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Статус операции слева с теплым янтарным LED индикатором -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <Ellipse x:Name="DotStatus" Width="8" Height="8" Fill="{StaticResource EmberDiode}" Margin="0,0,8,0"/>
                        <TextBlock x:Name="TxtStatus" Text="Готов к работе. Выберите каталог ТЖ или ЖР для диагностики." Foreground="{StaticResource PulpCream}" FontSize="12" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>

                    <!-- Полоса прогресса в процентах -->
                    <Grid Grid.Column="1" x:Name="PanelProgress" Visibility="Collapsed" Margin="14,0,18,0" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Border CornerRadius="0" Background="{StaticResource InkBlack}" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1" Padding="1" Height="8" Width="180" Margin="0,0,8,0">
                                <ProgressBar x:Name="ProgressBarParsing" Minimum="0" Maximum="100" Value="0"
                                             Foreground="{StaticResource EmberDiode}" Background="Transparent" BorderThickness="0"/>
                            </Border>
                            <TextBlock x:Name="TxtProgressPercent" Text="0%" Foreground="{StaticResource EmberDiode}" FontSize="11.5" FontWeight="SemiBold" Width="40" VerticalAlignment="Center" FontFamily="Consolas, monospace"/>
                            <TextBlock x:Name="TxtProgressDetails" Text="" Foreground="{StaticResource MidAsh}" FontSize="11" VerticalAlignment="Center"/>
                            <Button x:Name="BtnCancelGlobal" Content="ОТМЕНА" Click="BtnCancelParsing_Click" Background="#331414" Foreground="#FFFFFF" BorderBrush="#8A2B2B" BorderThickness="1" Padding="6,2" FontSize="11" Margin="8,0,0,0"/>
                        </StackPanel>
                    </Grid>

                    <!-- Дата редакции, ссылка на Telegram автора и кнопка быстрой очистки RAM справа -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button x:Name="BtnCleanMemory" Content="СБРОС RAM" Click="BtnCleanMemory_Click" Style="{StaticResource GhostButton}" Padding="8,2" FontSize="11" Margin="0,0,10,0" ToolTip="Принудительное освобождение неиспользуемой оперативной памяти и запуск сборщика мусора .NET (GC.Collect)."/>
                        <TextBlock x:Name="TxtAppBuildDate" Text="REV: 21.08.2026" Foreground="{StaticResource MidAsh}" FontSize="11" VerticalAlignment="Center" FontFamily="Consolas, monospace" Margin="0,0,10,0"/>
                        <Border Background="#161616" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1" CornerRadius="12" Padding="8,2" VerticalAlignment="Center" Cursor="Hand" MouseLeftButtonDown="TgLink_MouseLeftButtonDown" ToolTip="Открыть профиль автора в Telegram: @DarkSailas (https://t.me/DarkSailas)">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="TG:" Foreground="{StaticResource EmberDiode}" FontWeight="Bold" FontSize="10" Margin="0,0,4,0"/>
                                <TextBlock Text="@DarkSailas" Foreground="{StaticResource Vapor}" FontSize="11" TextDecorations="Underline"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- MODAL OVERLAY (Superlative Industrial Spec Frame) -->
        <Grid x:Name="OverlayDetails" Visibility="Collapsed" Background="#E6141414" MouseDown="OverlayDetails_MouseDown">
            <Border x:Name="ModalCard" Width="1020" MaxWidth="1280" MaxHeight="680" VerticalAlignment="Center" HorizontalAlignment="Center" Background="{StaticResource CharcoalPress}" BorderBrush="{StaticResource ActiveBorder}" BorderThickness="1" CornerRadius="0" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Шапка модального окна профиля записи -->
                    <Grid Grid.Row="0" Margin="0,0,0,12">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Верхняя строка: Заголовок слева, кнопки действий справа -->
                        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <Border Background="{StaticResource InkBlack}" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1" CornerRadius="3" Padding="6,2" Margin="0,0,8,0" VerticalAlignment="Center">
                                <TextBlock Text="EVENT" Foreground="{StaticResource EmberDiode}" FontWeight="Bold" FontSize="10" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock x:Name="TxtModalTitle" Text="ДЕТАЛИЗАЦИЯ СОБЫТИЯ 1С" FontSize="15" FontWeight="SemiBold" Foreground="{StaticResource Vapor}" VerticalAlignment="Center"/>
                        </StackPanel>

                        <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                            <Button x:Name="BtnCopyModalJson" Content="JSON" Click="BtnCopyJson_Click" Style="{StaticResource GhostButton}" Margin="0,0,6,0" ToolTip="Скопировать запись как JSON"/>
                            <Button x:Name="BtnCopyModalContext" Content="КОНТЕКСТ / SQL" Click="BtnCopyModalContext_Click" Style="{StaticResource GhostButton}" Margin="0,0,6,0" ToolTip="Скопировать текст контекста и SQL запроса"/>
                            <Button x:Name="BtnCopyModalAll" Content="СКОПИРОВАТЬ ВСЁ" Click="BtnCopyModalAll_Click" Style="{StaticResource PrimaryButton}" Margin="0,0,8,0" ToolTip="Скопировать все свойства и контекст в буфер обмена"/>
                            <Button x:Name="BtnCloseModal" Content="✕" Click="BtnCloseModal_Click" Width="30" Height="30" Padding="0" FontSize="13" FontWeight="SemiBold" Background="{StaticResource InkBlack}" Foreground="{StaticResource Vapor}" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1"/>
                        </StackPanel>

                        <!-- Нижняя строка: полный подзаголовок на всю ширину окна -->
                        <TextBlock x:Name="TxtModalSubTitle" Grid.Row="1" Grid.ColumnSpan="2" Text="2026-08-12 09:07:53.205 | Процесс: rphost_27380 (PID 5256)" Foreground="{StaticResource MidAsh}" FontSize="12" Margin="0,6,0,0" FontFamily="Consolas, monospace" TextTrimming="CharacterEllipsis"/>
                    </Grid>

                    <!-- Табы контента модального окна -->
                    <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0">
                        <TabItem Header="ФОРМАТИРОВАННЫЙ JSON">
                            <Border Background="{StaticResource InkBlack}" CornerRadius="0" Padding="10" Margin="0,6,0,0" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1">
                                <TextBox x:Name="TxtModalJson" IsReadOnly="True" IsReadOnlyCaretVisible="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Background="Transparent" Foreground="{StaticResource PulpCream}" FontFamily="Consolas, monospace" FontSize="12" BorderThickness="0"/>
                            </Border>
                        </TabItem>

                        <TabItem Header="КОНТЕКСТ &amp; SQL">
                            <Border Background="{StaticResource InkBlack}" CornerRadius="0" Padding="10" Margin="0,6,0,0" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1">
                                <TextBox x:Name="TxtModalContext" IsReadOnly="True" IsReadOnlyCaretVisible="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Background="Transparent" Foreground="{StaticResource PulpCream}" FontFamily="Consolas, monospace" FontSize="12" BorderThickness="0"/>
                            </Border>
                        </TabItem>

                        <TabItem Header="ВСЕ СВОЙСТВА (PROPERTIES)">
                            <Border Background="{StaticResource InkBlack}" CornerRadius="0" Padding="6" Margin="0,6,0,0" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1">
                                <DataGrid x:Name="GridModalProps" AutoGenerateColumns="False" IsReadOnly="True" CanUserSortColumns="True" SelectionMode="Extended" SelectionUnit="FullRow" MouseRightButtonDown="GridModalProps_MouseRightButtonDown" PreviewKeyDown="GridModalProps_PreviewKeyDown" Background="{StaticResource InkBlack}" RowBackground="#141414" AlternatingRowBackground="#1B1B1B" Foreground="{StaticResource PulpCream}" BorderThickness="0" HeadersVisibility="Column" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" ScrollViewer.CanContentScroll="True">
                                    <DataGrid.ContextMenu>
                                        <ContextMenu Background="#1C1C1C" Foreground="{StaticResource PulpCream}" BorderBrush="{StaticResource HairlineBorder}" BorderThickness="1">
                                            <MenuItem Header="Скопировать значение (Value)" Click="ModalPropsMenuCopyVal_Click" InputGestureText="ПКМ"/>
                                            <MenuItem Header="Скопировать имя свойства (Key)" Click="ModalPropsMenuCopyKey_Click"/>
                                            <MenuItem Header="Скопировать строку (Key = Value)" Click="ModalPropsMenuCopyRow_Click" InputGestureText="Ctrl+C"/>
                                            <Separator Background="{StaticResource HairlineBorder}"/>
                                            <MenuItem Header="Скопировать все свойства как JSON" Click="ModalPropsMenuCopyAllJson_Click"/>
                                        </ContextMenu>
                                    </DataGrid.ContextMenu>
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Свойство (Key)" Binding="{Binding Key}" Width="220" FontWeight="SemiBold"/>
                                        <DataGridTextColumn Header="Значение (Value)" Binding="{Binding Value}" Width="*"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Border>
                        </TabItem>
                    </TabControl>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
